# Контур КП — бренд и визуальная система

Версия 1.1 · 17.09.2026 · статус: **предложение**, ждёт согласования владельцем. Ответы владельца от 17.09.2026 — в §13.3.
Техническое имя: `kontur-kp`. Название рабочее: используется на время разработки и затем будет изменено (решение владельца). Товарный знак не проверялся, поэтому без ®/™.

Документ задаёт знак, логотип, цвет, шрифт, иконки и визуальные правила интерфейса. Код портала появится позже: стек фронтенда выбирается на этапе 01 (по умолчанию React + Vite + TS, CSS Modules + CSS Variables), иконки приложения генерируются на этапе 02. Правила `Odintsov/CLAUDE.md` («Адаптивность», «PWA», «Темы», «Анимации и переходы», «Логотипы и иконки») обязательны. При расхождении с ними прав `CLAUDE.md`, а этот документ нужно исправить.

## Кратко

| Решение | Значение |
|---|---|
| Идея знака | Лист документа, на котором фрагмент выделен контуром с ручками, как выделенный объект в САПР |
| Фирменный цвет | Петроль `#0F5B6E` (он же `--accent` светлой темы) |
| Шрифт | Inter 4.001 (SIL OFL 1.1), вариативный; моноширинный — JetBrains Mono (SIL OFL 1.1). Предлагается раздавать оба самим порталом, без CDN; окончательно — в ADR этапа 01 |
| Иконки | Lucide (ISC), сетка 24, штрих 2 |
| Файлы | `public/kontur-kp-favicon.svg`, `public/kontur-kp-logo-light.svg`, `public/kontur-kp-logo-dark.svg` |
| Контраст | 40 пар в каждой теме, нарушений нет. Минимумы: текст 5,29:1 (светлая) и 5,78:1 (тёмная), границы полей 3,41:1 и 3,58:1 |

---

## 1. Идея и тон

**Идея.** Название «Контур» понимается буквально: это контур вокруг фрагмента документа. Каждый вывод портала опирается на доказательство: документ, редакцию, страницу или лист, блок. Знак показывает именно это: лист, строки текста и один фрагмент, взятый в рамку с ручками. Более жирный фрагмент — это факт, на котором держится вывод.

**Тон:**

- **Точность.** Числа с единицами, НДС, валютой и версией. Никакого округления «для красоты» и размытых «всё в порядке».
- **Проверяемость.** У значения виден источник. Сгенерированное моделью помечено как производное. Отсутствие данных называется точно (I07).
- **Спокойствие.** Нейтральные поверхности и один акцентный цвет. Статусные цвета — только у статусов. Никаких праздничных эффектов, красный — только по делу.

**Как пишем в интерфейсе:**

- Кнопка называет действие: «Согласовать…», «Выпустить…», «Зарегистрировать отправку». Кнопок «ОК» нет.
- Факт вместо оценки: «Распознано 212 из 240 стр.», а не «Почти готово».
- Не обещаем полноту: «Замечаний не найдено в проверенном объёме» и рядом охват проверки.
- Пишем «ё». Кавычки — «ёлочки», тире — «—» с пробелами. Между числом и единицей неразрывный пробел: `12 м³`, `№ 14`.
- Сокращения: «ред.», «с.», «л.», «п.», «шт.», «компл.».

**Не используем:** фотографии и иллюстрации с людьми, эмодзи, градиенты, стекло и размытие, маскотов.

---

## 2. Логотип

### 2.1. Знак — `public/kontur-kp-favicon.svg`

Холст 512×512, `viewBox="0 0 512 512"`. Фон непрозрачный, во весь холст (под maskable-иконку и iOS). Ниже описано ровно то, что нарисовано в файле.

| Элемент | Геометрия | Цвет |
|---|---|---|
| Фон | Квадрат 0…512 × 0…512, без скругления | `#0F5B6E` |
| Лист | Прямоугольник x 144…368, y 96…416 (224×320, ≈1:1,43, пропорция А4), углы острые | `#FFFFFF` |
| Строка сверху | Линия y = 152, x 192…336, толщина 16 | `#0F5B6E` |
| Фрагмент | Линия y = 256, x 208…304, толщина 32 | `#0F5B6E` |
| Контур | Прямоугольник по осевой x 184…328, y 216…296, толщина 16 (снаружи 176…336 × 208…304), углы острые | `#0F5B6E` |
| Ручки | 4 квадрата 32×32 с центрами в углах контура: (184; 216), (328; 216), (184; 296), (328; 296) | `#0F5B6E` |
| Строка снизу | Линия y = 360, x 192…304, толщина 16 | `#0F5B6E` |

Как построен знак:

- **Сетка 16.** Края линий, контура и фрагмента кратны 16. Поэтому на 32 px они ложатся на пиксели: рендер 32 px чёткий, только у ручек края смягчены.
- **Симметрия.** Лист, контур и фрагмент симметричны относительно x = 256. Контур и фрагмент центрированы по y = 256. От краёв листа до крайних строк по 48. Строки текста выровнены влево по x = 192.
- **Безопасная зона maskable.** Самые дальние точки (углы листа) удалены от центра на 195,3. Это меньше 204,8, то есть 40 % от 512. Значит, всё значимое лежит в круге диаметром 80 %. Проверено рендером с круглой маской.
- **Два цвета.** Фирменный и белый. Статусных цветов в знаке нет.

### 2.2. Логотип — `public/kontur-kp-logo-light.svg` и `public/kontur-kp-logo-dark.svg`

Холст `viewBox="0 0 391 96"`. Геометрия у обоих файлов одинаковая, отличаются только цвета.

| Часть | Геометрия |
|---|---|
| Плитка | Квадрат 96×96, `rx="21"` (≈22 %) |
| Знак на плитке | Тот же рисунок, что в §2.1, с `transform="scale(0.1875)"` (96/512); фон знака заменён скруглённой плиткой |
| Отступ плитка → надпись | 24 |
| Надпись «Контур КП» | Переведена в кривые (`<path>`): тегов `<text>` нет, от шрифтов не зависит |
| Шрифт надписи | Inter 4.001, SemiBold (wght 600), оптический размер opsz 32; кернинг из шрифта (GPOS, `cyrl`); трекинг −1 %; пробел между словами 0,30 em |
| Метрики надписи | Высота прописных 40 (кегль ≈54,98). Базовая линия y = 65, верх прописных y = 25, низ «у» и «р» y ≈ 76,2. Надпись начинается на x = 120, «Контур» заканчивается на x ≈ 299,6, «КП» занимает x ≈ 321,2…390,0 |

Как получены кривые: из пакета `@fontsource-variable/inter` 5.3.0 через fontTools 4.63 собран статический инстанс (wght 600, opsz 32), глифы разложены с кернингом в opentype.js 2.0.0. Лицензия OFL позволяет использовать контуры глифов в логотипе. Источник указан в комментарии внутри SVG.

| Вариант | Файл | Плитка | Лист | Строки, контур, ручки | «Контур» | «КП» | Где применять |
|---|---|---|---|---|---|---|---|
| Светлый | `kontur-kp-logo-light.svg` | `#0F5B6E` | `#FFFFFF` | `#0F5B6E` | `#16202A` (`--text`) | `#0F5B6E` (`--accent`) | На `--bg` и `--surface` светлой темы |
| Тёмный | `kontur-kp-logo-dark.svg` | `#1B7489` | `#E6ECF1` | `#1B7489` | `#E6ECF1` (`--text`) | `#5BB8CE` (`--accent`) | На `--bg` и `--surface` тёмной темы |
| Монохромный | Отдельного файла нет, есть рецепт | Цвет C | Вырез (виден фон) | C | C | C | Печать, одноцветные носители |

**Почему в тёмном логотипе плитка светлее фирменной:** фирменная `#0F5B6E` на `#0E1318` даёт только 2,44:1, а `#1B7489` — 3,47:1. Лист на тёмной плитке не белый, а `#E6ECF1`, чтобы не слепить. Знаки на листе против листа дают 4,51:1.

**Рецепт монохрома.** Плитка, строки, контур, ручки и надпись окрашены одним цветом C. Лист не закрашивается: это вырез в плитке (`fill-rule="evenodd"`), и сквозь него виден фон. Проверено временными рендерами: `#16202A` на белом, `#FFFFFF` на `#0E1318`, `#16202A` на жёлтом — всё читается. Файл создаём, когда появится реальный носитель.

### 2.3. Охранное поле

**X = ¼ высоты плитки.** В файле логотипа это 24 из 96. Со всех сторон логотипа и отдельного знака оставляем не меньше X. Для логотипа высотой 32 px это 8 px.

### 2.4. Минимальные размеры

Проверено рендером в Edge и в sharp/librsvg.

| Что | Минимум | Что видно на минимуме |
|---|---|---|
| Логотип (знак + надпись) | Высота 24 px | Прописные 10 px, надпись читается. Ниже 24 px ставим только знак |
| Знак в интерфейсе | 24 px | Видны лист, контур и фрагмент |
| Знак как favicon | 16 px | Детали сливаются, остаётся силуэт листа с тёмной полосой. Для вкладки есть укрупнённый `favicon.svg` (§12) |

### 2.5. Запреты

- Не перекрашивать плитку в статусные цвета (`--danger`, `--warning`, `--success`, `--derived`): их прочтут как статус.
- Не добавлять к знаку галочку, самолётик, облако или замок. Это иконки статусов (§4, §8).
- Не писать «Контур.КП», «КонтурКП», «КОНТУР КП», не ставить ®/™. Написание через точку похоже на продуктовую линейку СКБ «Контур» (см. §13).
- Не делать буквенную плитку «КП»: такое сокращение широко известно как логотип издания «Комсомольская правда».
- Не использовать красный как фирменный цвет: он закреплён за `--danger`.
- Не набирать надпись живым текстом или другим шрифтом. Не растягивать, не поворачивать, не менять пропорции плитки и надписи. Не добавлять тени, обводки, градиенты.
- Не менять цвета «Контур» и «КП» местами. Не ставить светлый вариант на тёмный фон и наоборот, не ставить логотип на фото и пёстрый фон.
- Не вставлять бренд портала в файлы для заказчика. КП выпускается по шаблонам компании и заказчика (спецификация, §8–9): бренд портала только внутренний.

### 2.6. Логотип в интерфейсе

- Шапка: логотип высотой 32 px (≥768 px) или 28 px (<768 px). На экранах уже 390 px — только знак 28 px и название страницы.
- Тему выбирает пользователь через `data-theme`, а `<picture media="(prefers-color-scheme)">` этот выбор не видит. Поэтому в разметке оба `<img alt="Контур КП">`, лишний скрываем через CSS:

```css
/* Header.module.css */
.logoDark {
  display: none;
}

:global(:root[data-theme='dark']) .logoLight {
  display: none;
}

:global(:root[data-theme='dark']) .logoDark {
  display: block;
}
```

---

## 3. Цвет

### 3.1. Принципы

1. Цвета задаются только токенами. Hex допустим в `src/index.css` (определение токенов), `manifest.json`, `index.html` (`theme-color`) и SVG-ассетах. В компонентах hex запрещён.
2. Акцент один — петроль. Он у действий, ссылок и выделения. Статусные цвета (`danger`, `warning`, `success`, `info`, `derived`) — только у статусов.
3. Цвет никогда не единственный носитель смысла. Его всегда дублируют текст и иконка, при необходимости — форма рамки или штриховка (§4).
4. Мягкий фон `*-soft` сочетается только со своим передним цветом: `--warning` на `--warning-soft` и т. д. Все такие пары измерены в §3.4.
5. `--border-subtle` (≈1,4:1) — декоративная линия: разделители строк, рамки карточек. Единственной границей интерактивного элемента она быть не может, для полей ввода есть `--border` (≥3:1).

### 3.2. Токены

| Токен | Светлая | Тёмная | Назначение |
|---|---|---|---|
| `--bg` | `#F4F6F8` | `#0E1318` | Фон страницы |
| `--surface` | `#FFFFFF` | `#151C23` | Карточки, таблицы, шапка, диалоги |
| `--surface-2` | `#EDF1F4` | `#1C252E` | Шапка таблицы, наведение, цитаты, вторичные панели |
| `--text` | `#16202A` | `#E6ECF1` | Основной текст и иконки |
| `--muted` | `#56626E` | `#9AA7B3` | Вторичный текст, неактивные шаги |
| `--border` | `#768390` | `#6E7B88` | Границы полей и элементов управления (≥3:1) |
| `--border-subtle` | `#D6DDE3` | `#2A3540` | Декоративные разделители |
| `--accent` | `#0F5B6E` | `#5BB8CE` | Основная кнопка, ссылки, выделение, «Готов к проверке», «Согласовано» |
| `--accent-hover` | `#0B4857` | `#7CC8DA` | Наведение на основную кнопку |
| `--accent-contrast` | `#FFFFFF` | `#06232B` | Текст и иконка на `--accent` |
| `--accent-soft` | `#E2F0F4` | `#123440` | Выбранная строка, бейдж «Готов к проверке», решение человека |
| `--danger` | `#B42318` | `#F2857A` | Ошибка, критичность, блокировка, удержание |
| `--danger-contrast` | `#FFFFFF` | `#2A0703` | Текст на `--danger` (опасная кнопка в диалоге) |
| `--danger-soft` | `#FCEBE9` | `#3A1714` | Фон бейджа и баннера опасности, удалённая строка сравнения |
| `--warning` | `#8A5100` | `#E9B45A` | Предупреждение, «нужны исправления», неполнота, устаревание |
| `--warning-soft` | `#FDF1DA` | `#35270F` | Фон предупреждения, изменённая строка сравнения |
| `--success` | `#18703D` | `#62C48E` | «Принят», «Проверено вживую», «Отправлено» |
| `--success-soft` | `#E6F4EB` | `#12301F` | Фон успеха, добавленная строка сравнения |
| `--info` | `#2B5FB8` | `#86AEF2` | «В работе», «Размещено», информационные баннеры |
| `--info-soft` | `#E8EFFA` | `#15253F` | Фон информационных бейджей и баннеров |
| `--derived` | `#6B45B5` | `#BBA2F2` | Производное от модели (I06) |
| `--derived-soft` | `#F0EBF9` | `#261C3C` | Фон блоков, сгенерированных моделью |
| `--focus` | `#2563EB` | `#7DB0FF` | Кольцо фокуса `:focus-visible` |
| `--hatch` | `#DCE2E7` | `#2A3540` | Штриховка: фикстуры, незавершённая обработка |
| `--scrim` | `rgba(12, 17, 22, 0.48)` | `rgba(0, 0, 0, 0.6)` | Затемнение под диалогом и нижним листом |
| `--shadow-1` | `0 1px 2px rgba(16, 24, 32, 0.08)` | `0 1px 2px rgba(0, 0, 0, 0.5)` | Карточки, липкая шапка таблицы |
| `--shadow-2` | `0 6px 16px rgba(16, 24, 32, 0.12)` | `0 8px 24px rgba(0, 0, 0, 0.55)` | Всплывающие окна, тосты, диалоги |

Фирменные цвета для ассетов (не CSS-токены): `#0F5B6E` — плитка знака, иконки приложения, `background_color` манифеста; `#1B7489` — плитка тёмного логотипа.

### 3.3. Токены в `src/index.css`

```css
:root,
:root[data-theme='light'] {
  color-scheme: light;
  --bg: #F4F6F8;
  --surface: #FFFFFF;
  --surface-2: #EDF1F4;
  --text: #16202A;
  --muted: #56626E;
  --border: #768390;
  --border-subtle: #D6DDE3;
  --accent: #0F5B6E;
  --accent-hover: #0B4857;
  --accent-contrast: #FFFFFF;
  --accent-soft: #E2F0F4;
  --danger: #B42318;
  --danger-contrast: #FFFFFF;
  --danger-soft: #FCEBE9;
  --warning: #8A5100;
  --warning-soft: #FDF1DA;
  --success: #18703D;
  --success-soft: #E6F4EB;
  --info: #2B5FB8;
  --info-soft: #E8EFFA;
  --derived: #6B45B5;
  --derived-soft: #F0EBF9;
  --focus: #2563EB;
  --hatch: #DCE2E7;
  --scrim: rgba(12, 17, 22, 0.48);
  --shadow-1: 0 1px 2px rgba(16, 24, 32, 0.08);
  --shadow-2: 0 6px 16px rgba(16, 24, 32, 0.12);
}

:root[data-theme='dark'] {
  color-scheme: dark;
  --bg: #0E1318;
  --surface: #151C23;
  --surface-2: #1C252E;
  --text: #E6ECF1;
  --muted: #9AA7B3;
  --border: #6E7B88;
  --border-subtle: #2A3540;
  --accent: #5BB8CE;
  --accent-hover: #7CC8DA;
  --accent-contrast: #06232B;
  --accent-soft: #123440;
  --danger: #F2857A;
  --danger-contrast: #2A0703;
  --danger-soft: #3A1714;
  --warning: #E9B45A;
  --warning-soft: #35270F;
  --success: #62C48E;
  --success-soft: #12301F;
  --info: #86AEF2;
  --info-soft: #15253F;
  --derived: #BBA2F2;
  --derived-soft: #261C3C;
  --focus: #7DB0FF;
  --hatch: #2A3540;
  --scrim: rgba(0, 0, 0, 0.6);
  --shadow-1: 0 1px 2px rgba(0, 0, 0, 0.5);
  --shadow-2: 0 8px 24px rgba(0, 0, 0, 0.55);
}

:focus-visible {
  outline: 2px solid var(--focus);
  outline-offset: 2px;
}
```

Типографика, отступы и движение в токенах — в §5, §7 и §10.

### 3.4. Контраст (WCAG 2.x)

Метод: относительная яркость и отношение контраста по WCAG 2.x. Считал скрипт на Python 3.14 (ядро скрипта — в приложении A) по значениям токенов из §3.2. Пороги: текст и иконки статусов ≥4,5:1, границы полей, кольцо фокуса и заливка кнопки относительно фона ≥3:1. Итог: **40 пар в каждой теме, нарушений 0.**

| Пара `передний / фон` | Назначение | Порог | Светлая тема | Тёмная тема |
|---|---|---:|---|---|
| `--text` / `--bg` | основной текст | 4.5:1 | `#16202A` / `#F4F6F8` → **15.21:1** ✓ | `#E6ECF1` / `#0E1318` → **15.67:1** ✓ |
| `--text` / `--surface` | основной текст | 4.5:1 | `#16202A` / `#FFFFFF` → **16.48:1** ✓ | `#E6ECF1` / `#151C23` → **14.43:1** ✓ |
| `--text` / `--surface-2` | основной текст | 4.5:1 | `#16202A` / `#EDF1F4` → **14.51:1** ✓ | `#E6ECF1` / `#1C252E` → **13.03:1** ✓ |
| `--muted` / `--bg` | вторичный текст | 4.5:1 | `#56626E` / `#F4F6F8` → **5.76:1** ✓ | `#9AA7B3` / `#0E1318` → **7.60:1** ✓ |
| `--muted` / `--surface` | вторичный текст | 4.5:1 | `#56626E` / `#FFFFFF` → **6.24:1** ✓ | `#9AA7B3` / `#151C23` → **7.00:1** ✓ |
| `--muted` / `--surface-2` | вторичный текст | 4.5:1 | `#56626E` / `#EDF1F4` → **5.49:1** ✓ | `#9AA7B3` / `#1C252E` → **6.32:1** ✓ |
| `--accent` / `--bg` | ссылка, акцентный текст | 4.5:1 | `#0F5B6E` / `#F4F6F8` → **7.07:1** ✓ | `#5BB8CE` / `#0E1318` → **8.17:1** ✓ |
| `--accent` / `--surface` | ссылка, акцентный текст | 4.5:1 | `#0F5B6E` / `#FFFFFF` → **7.66:1** ✓ | `#5BB8CE` / `#151C23` → **7.52:1** ✓ |
| `--accent` / `--surface-2` | ссылка, акцентный текст | 4.5:1 | `#0F5B6E` / `#EDF1F4` → **6.74:1** ✓ | `#5BB8CE` / `#1C252E` → **6.79:1** ✓ |
| `--accent-contrast` / `--accent` | подпись основной кнопки | 4.5:1 | `#FFFFFF` / `#0F5B6E` → **7.66:1** ✓ | `#06232B` / `#5BB8CE` → **7.16:1** ✓ |
| `--accent-contrast` / `--accent-hover` | подпись кнопки при наведении | 4.5:1 | `#FFFFFF` / `#0B4857` → **10.10:1** ✓ | `#06232B` / `#7CC8DA` → **8.68:1** ✓ |
| `--text` / `--accent-soft` | текст выбранной строки | 4.5:1 | `#16202A` / `#E2F0F4` → **14.13:1** ✓ | `#E6ECF1` / `#123440` → **11.08:1** ✓ |
| `--accent` / `--accent-soft` | бейдж «Готов к проверке» | 4.5:1 | `#0F5B6E` / `#E2F0F4` → **6.57:1** ✓ | `#5BB8CE` / `#123440` → **5.78:1** ✓ |
| `--danger` / `--surface` | текст и иконка ошибки | 4.5:1 | `#B42318` / `#FFFFFF` → **6.57:1** ✓ | `#F2857A` / `#151C23` → **6.88:1** ✓ |
| `--danger` / `--danger-soft` | бейдж опасности | 4.5:1 | `#B42318` / `#FCEBE9` → **5.70:1** ✓ | `#F2857A` / `#3A1714` → **6.41:1** ✓ |
| `--danger-contrast` / `--danger` | подпись опасной кнопки с заливкой | 4.5:1 | `#FFFFFF` / `#B42318` → **6.57:1** ✓ | `#2A0703` / `#F2857A` → **7.44:1** ✓ |
| `--warning` / `--surface` | текст и иконка предупреждения | 4.5:1 | `#8A5100` / `#FFFFFF` → **6.45:1** ✓ | `#E9B45A` / `#151C23` → **9.11:1** ✓ |
| `--warning` / `--warning-soft` | бейдж предупреждения | 4.5:1 | `#8A5100` / `#FDF1DA` → **5.76:1** ✓ | `#E9B45A` / `#35270F` → **7.69:1** ✓ |
| `--success` / `--surface` | текст и иконка успеха | 4.5:1 | `#18703D` / `#FFFFFF` → **6.13:1** ✓ | `#62C48E` / `#151C23` → **8.02:1** ✓ |
| `--success` / `--success-soft` | бейдж успеха | 4.5:1 | `#18703D` / `#E6F4EB` → **5.40:1** ✓ | `#62C48E` / `#12301F` → **6.68:1** ✓ |
| `--info` / `--surface` | текст и иконка «в работе» | 4.5:1 | `#2B5FB8` / `#FFFFFF` → **6.12:1** ✓ | `#86AEF2` / `#151C23` → **7.65:1** ✓ |
| `--info` / `--info-soft` | бейдж «в работе» | 4.5:1 | `#2B5FB8` / `#E8EFFA` → **5.29:1** ✓ | `#86AEF2` / `#15253F` → **6.83:1** ✓ |
| `--derived` / `--surface` | метка «производное» | 4.5:1 | `#6B45B5` / `#FFFFFF` → **6.68:1** ✓ | `#BBA2F2` / `#151C23` → **7.82:1** ✓ |
| `--derived` / `--derived-soft` | чип «производное» | 4.5:1 | `#6B45B5` / `#F0EBF9` → **5.71:1** ✓ | `#BBA2F2` / `#261C3C` → **7.28:1** ✓ |
| `--text` / `--danger-soft` | текст в баннере | 4.5:1 | `#16202A` / `#FCEBE9` → **14.28:1** ✓ | `#E6ECF1` / `#3A1714` → **13.43:1** ✓ |
| `--text` / `--warning-soft` | текст в баннере | 4.5:1 | `#16202A` / `#FDF1DA` → **14.73:1** ✓ | `#E6ECF1` / `#35270F` → **12.17:1** ✓ |
| `--text` / `--success-soft` | текст в баннере | 4.5:1 | `#16202A` / `#E6F4EB` → **14.52:1** ✓ | `#E6ECF1` / `#12301F` → **12.01:1** ✓ |
| `--text` / `--info-soft` | текст в баннере | 4.5:1 | `#16202A` / `#E8EFFA` → **14.25:1** ✓ | `#E6ECF1` / `#15253F` → **12.89:1** ✓ |
| `--text` / `--derived-soft` | текст блока модели | 4.5:1 | `#16202A` / `#F0EBF9` → **14.09:1** ✓ | `#E6ECF1` / `#261C3C` → **13.42:1** ✓ |
| `--muted` / `--warning-soft` | вторичный текст в баннере | 4.5:1 | `#56626E` / `#FDF1DA` → **5.57:1** ✓ | `#9AA7B3` / `#35270F` → **5.90:1** ✓ |
| `--text` / `--hatch` | подпись на штриховке «фикстуры» | 4.5:1 | `#16202A` / `#DCE2E7` → **12.62:1** ✓ | `#E6ECF1` / `#2A3540` → **10.49:1** ✓ |
| `--border` / `--bg` | граница поля ввода | 3:1 | `#768390` / `#F4F6F8` → **3.58:1** ✓ | `#6E7B88` / `#0E1318` → **4.31:1** ✓ |
| `--border` / `--surface` | граница поля ввода | 3:1 | `#768390` / `#FFFFFF` → **3.87:1** ✓ | `#6E7B88` / `#151C23` → **3.97:1** ✓ |
| `--border` / `--surface-2` | граница поля ввода | 3:1 | `#768390` / `#EDF1F4` → **3.41:1** ✓ | `#6E7B88` / `#1C252E` → **3.58:1** ✓ |
| `--focus` / `--bg` | кольцо фокуса | 3:1 | `#2563EB` / `#F4F6F8` → **4.77:1** ✓ | `#7DB0FF` / `#0E1318` → **8.47:1** ✓ |
| `--focus` / `--surface` | кольцо фокуса | 3:1 | `#2563EB` / `#FFFFFF` → **5.17:1** ✓ | `#7DB0FF` / `#151C23` → **7.79:1** ✓ |
| `--focus` / `--surface-2` | кольцо фокуса | 3:1 | `#2563EB` / `#EDF1F4` → **4.55:1** ✓ | `#7DB0FF` / `#1C252E` → **7.04:1** ✓ |
| `--accent` / `--bg` | заливка кнопки к фону | 3:1 | `#0F5B6E` / `#F4F6F8` → **7.07:1** ✓ | `#5BB8CE` / `#0E1318` → **8.17:1** ✓ |
| `--accent` / `--surface` | заливка кнопки к фону | 3:1 | `#0F5B6E` / `#FFFFFF` → **7.66:1** ✓ | `#5BB8CE` / `#151C23` → **7.52:1** ✓ |
| `--danger` / `--bg` | граница опасной кнопки к фону | 3:1 | `#B42318` / `#F4F6F8` → **6.07:1** ✓ | `#F2857A` / `#0E1318` → **7.48:1** ✓ |

Справочно (порогов нет, но цифры измерены): лист / плитка знака `#FFFFFF` / `#0F5B6E` — 7,66:1; тёмный логотип: лист / плитка 4,51:1, плитка / `--bg` 3,47:1, плитка / `--surface` 3,20:1; `--border-subtle` / `--surface` — 1,37:1 (светлая) и 1,38:1 (тёмная); `--hatch` / `--surface` — 1,31:1 и 1,38:1. Последние два — декор, поэтому смысл всегда дублирует текст и иконка.

После **любого** изменения токенов таблицу пересчитываем.

---

## 4. Семантика домена

Общее правило: **состояние = подпись + иконка**, к ним добавляется цвет, а где нужно — форма рамки или штриховка. Состояний «только иконкой» и «только цветом» нет. Всплывающая подсказка подпись не заменяет. Для скринридера текст совпадает с видимой подписью.

Коды взяты из спецификации. Русские подписи — предложение, их утверждает владелец.

### 4.1. Статусы интеграций

| Код | Подпись | Иконка | Цвет и форма | Что показать рядом |
|---|---|---|---|---|
| `NOT_IMPLEMENTED` | «Не реализовано» | `circle-dashed` | `--muted`, без фона, пунктирная рамка `--border` | — |
| `VERIFIED_FIXTURE` | «Проверено на фикстурах» | `flask-conical` | `--text` на штриховке `--hatch`, пунктирная рамка `--border` | Дата прогона и «живая проверка не выполнялась» |
| `VERIFIED_LIVE` | «Проверено вживую» | `plug-zap` | `--success` на `--success-soft`, сплошная рамка `--success` | Дата последней живой проверки, против какой системы |
| `BLOCKED_EXTERNAL` | «Блокировка внешней системы» | `unplug` | `--danger` на `--danger-soft`, сплошная рамка | Причина, с какого времени, кто должен снять |

**Фикстура не выглядит как живая проверка.** Их различают четыре канала: подпись, иконка (колба против вилки), цвет (нейтральный против зелёного) и форма (штриховка с пунктиром против сплошной рамки). Фикстура никогда не бывает зелёной, не получает галочку и `plug-zap`. В сводках «готово к пилоту» фикстуры считаются отдельной строкой и в «готово» не суммируются. Блокировка не переименовывается в успех.

### 4.2. Статусы этапов

| Код | Подпись | Иконка | Цвет | Дополнительно |
|---|---|---|---|---|
| `NOT_STARTED` | «Не начат» | `circle` | `--muted`, рамка `--border-subtle`, без фона | — |
| `IN_PROGRESS` | «В работе» | `contrast` (полузаполненный круг) | `--info` на `--info-soft` | Кто ведёт |
| `READY_FOR_REVIEW` | «Готов к проверке» | `eye` | `--accent` на `--accent-soft` | Проверяемый коммит или версия |
| `CHANGES_REQUIRED` | «Нужны исправления» | `undo-2` | `--warning` на `--warning-soft` | Число замечаний |
| `BLOCKED` | «Заблокирован» | `ban` | `--danger` на `--danger-soft` | Причина |
| `ACCEPTED` | «Принят» | `circle-check-big` | `--success` на `--success-soft` | Кем и когда |

### 4.3. Цепочка выпуска (I01)

Закрытие расчёта, согласование, размещение и отправка — **четыре разных события**. На десктопе это горизонтальная цепочка, на смартфоне — вертикальный список. У каждого шага своя иконка, подпись и факты.

| Событие | Подпись: есть / нет | Иконка | Цвет | Обязательные факты |
|---|---|---|---|---|
| Закрытие расчёта | «Расчёт закрыт» / «Расчёт не закрыт» | `lock` | `--text` | Система (TenderHub), ревизия, время |
| Согласование | «Согласовано» / «Не согласовано» | `stamp` | `--accent` | Кто, когда, какой кандидат (ревизии, число файлов) |
| Размещение | «Размещено 2/2» / «Не размещено» | `hard-drive-upload` | `--info` | Каждое назначение (Яндекс Диск, SMB) отдельно, совпадение хэшей |
| Отправка | «Отправлено заказчику» / «Отправка не зарегистрирована» | `send` | `--success` | Дата, адресаты, канал, основание (письмо или файл), release ID |

Правила:

- **«Размещено» не похоже на «Отправлено».** У размещения другая иконка (диск со стрелкой, а не самолётик), другой цвет (`--info`, а не `--success`), подпись без слова «отправлено» и счётчик назначений. У отправки обязательны адресаты и основание. Upload заказчику не показывается как отправка.
- Частичное размещение — «Размещено 1/2» в `--warning` с названием неудавшегося назначения. Зелёным или «готовым» оно не бывает (§9 спецификации, I14).
- Несовершённый шаг: `--muted`, подпись «не …», иконка в пунктирном круге. Одного серого цвета мало, нужен текст.
- Статус шага берётся только из его события. Если поздний шаг есть без раннего, показываем как есть, с предупреждением. Автоматически ничего не «дорисовываем».
- **Удержание отправки** (I04). Над цепочкой — баннер `--danger-soft` с иконкой `octagon-pause`: «Отправка удержана: после согласования поступил „Ответ на вопрос № 14“», действие «Оценить применимость». Шаг «Отправка» показывает «Удержано» с той же иконкой. Прежнее согласование при этом остаётся видимым.

### 4.4. Критичность и уверенность — разные каналы

**Критичность** передаётся цветом, формой иконки и подписью. Место — бейдж слева в заголовке карточки и левая полоса 4 px.

| Уровень (предложение) | Подпись | Иконка | Цвет |
|---|---|---|---|
| `critical` | «Критическое» | `octagon-alert` (восьмиугольник) | `--danger` |
| `major` | «Существенное» | `triangle-alert` (треугольник) | `--warning` |
| `minor` | «Незначительное» | `circle-alert` (круг) | `--muted` |

**Уверенность и степень подтверждения** — монохромный индикатор из трёх сегментов цвета `--text` плюс подпись. Место — справа в заголовке карточки. Статусные цвета он не использует никогда.

| Степень | Индикатор | Подпись | Иконка |
|---|---|---|---|
| Низкая | ■□□ | «Уверенность: низкая · предположение модели» | `sparkles` |
| Средняя | ■■□ | «Уверенность: средняя · предположение модели» | `sparkles` |
| Высокая | ■■■ | «Уверенность: высокая · предположение модели» | `sparkles` |
| Подтверждено человеком | ■■■ | «Подтверждено: И. Петров, 17.09.2026» | `user-check` |

Высокая уверенность модели не выглядит как подтверждение (I11): другая иконка, в подписи нет слова «подтверждено», а автор указан только у человеческого подтверждения. Денежное влияние показывается с расчётным основанием или честно: «Не рассчитано — нет расчётного основания» (спецификация, §7).

### 4.5. Типы данных (I06)

| Тип | Метка | Иконка | Оформление блока | Производное |
|---|---|---|---|---|
| Текст документа (ПД, ТЗ, договор, письмо) | «Текст документа» | `file-text` | Левая линия 3 px `--border`, фон `--surface-2`, обычный текст | Нет |
| Результат OCR | «OCR · качество 0,93» | `scan-text` | Точечная (`dotted`) рамка `--border`, у метки качество и полнота распознавания | Машинное распознавание; при низком качестве — предупреждение |
| Транскрипция переговоров | «Транскрипция · 00:14:32» | `audio-lines` | Как у OCR, у метки таймкод | Машинное распознавание; устное — не согласие (I08) |
| Описание изображения моделью | «Описание модели · производное» | `sparkles` | Пунктирная рамка `--derived`, фон `--derived-soft` | **Да** |
| Подсказка переговорного сервиса | «Подсказка переговоров · производное» | `lightbulb` | Как у описания модели, плюс таймкод | **Да** |
| Решение человека | «Решение: И. Петров, 17.09.2026» | `user-check` | Сплошная рамка 2 px `--accent`, фон `--accent-soft` | Нет |

Правила:

- Всё, что сгенерировала модель, несёт слово «производное» и пунктирную рамку `--derived` до явного действия человека. Курсив как единственный признак не годится.
- Производный текст не получает оформление решения. Перенос производного текста в решение — отдельное действие, и у решения остаётся ссылка «на основе: подсказка переговоров от …».
- Замечание, предложенное моделью, остаётся чипом «Предложено моделью» (стиль производного), пока человек его не примет (I12).

### 4.6. Отсутствие, расхождения, актуальность (I07, I04, I18)

| Состояние | Подпись | Иконка | Цвет и форма | Что обязательно показать |
|---|---|---|---|---|
| Не найдено | «Не найдено» | `search-x` | Нейтрально: `--text`, рамка `--border-subtle` | Где искали: набор источников, число документов и страниц, запрос |
| Не предусмотрено | «Не предусмотрено» | `circle-slash` | Нейтрально | Цитата-основание, где это сказано (§9.5) |
| Нет покрытия в расчёте | «Нет покрытия в расчёте» | `link-2-off` | `--warning` | Требование и ревизия расчёта |
| Нет данных | «Нет данных» | `file-question-mark` | Нейтрально | Какой источник не получен или недоступен |
| Неполная обработка | «Неполная обработка» | `file-exclamation-point` | `--warning`, пунктирная рамка, индикатор со штриховкой `--hatch` | «Обработано N из M», что пропущено; слов «проверено полностью» нет (I18) |
| Подтверждённое расхождение | «Расхождение подтверждено» | `equal-not` | `--danger`, сплошная рамка | Обе стороны с цитатами, кто подтвердил |
| Подтверждение устарело | «Подтверждение устарело» | `clock-alert` | `--warning`, пунктирная рамка | Что изменилось (ред. 3 → ред. 4); ручной текст сохранён (спецификация, §8) |
| Удержание отправки | «Отправка удержана» | `octagon-pause` | Баннер `--danger` | Причина (новый источник), действие (§4.3) |

«Не найдено» и «Не предусмотрено» не сливаются в одно «Нет». У «Не предусмотрено» всегда есть цитата, у «Не найдено» — область поиска. Прочерк «—» в таблицах означает только «не применимо». Отсутствие данных всегда показывается явной меткой из этой таблицы.

### 4.7. Прочие маркеры

| Маркер | Подпись | Иконка | Оформление |
|---|---|---|---|
| Исторический режим выпуска (I05) | «Режим выпуска от 16.09.2026: поздние документы скрыты» | `rotate-ccw-clock` | Постоянная полоса `--info-soft` под шапкой, пока режим активен |
| Ручное изменение значения | «Изменено вручную: А. Смирнов» | `pencil` | Иконка 16 рядом со значением, автор и время во всплывающем окне |
| Внутренние данные (не для заказчика) | «Внутреннее» | `eye-off` | Нейтральный бейдж у себестоимости, маржи, замечаний (спецификация, §9) |
| Данные из деградировавшей интеграции | «Данные на 13:58» | `clock-alert` | `--warning` у раздела или значения (§9.10) |

---

## 5. Типографика

### 5.1. Шрифты

**Основной — Inter** (SIL OFL 1.1), вариативный, ось wght 100…900. Почему он:

- Полная кириллица, включая «№». Шрифт рисовался для экрана, у него крупная высота строчных, поэтому плотные таблицы 13–14 px хорошо читаются.
- Есть табличные цифры `tnum`. Проверено в файлах `@fontsource-variable/inter` 5.3.0 (подмножества cyrillic и latin) и рендером: `font-variant-numeric: tabular-nums` работает.
- Открытая лицензия. Раздаётся самим порталом: npm-пакет Fontsource кладёт woff2 в сборку, внешних запросов нет. Способ фиксируется в ADR этапа 01.
- Та же гарнитура в логотипе, поэтому бренд и интерфейс согласованы.
- Вес: `wght` cyrillic 18,7 КБ + latin 48,3 КБ. Браузер качает подмножество по `unicode-range` только при необходимости. Если нужен оптический размер (`opsz`, файл `opsz.css`), то 29,6 + 72,9 КБ — только для крупных заголовков и по решению этапа 01.

**Ограничения сборки Fontsource/Google Fonts (проверено fontTools):**

- Нет OpenType-функций `zero`, `ss01…`, `cv01…`, `case`: перечёркнутого нуля и различения l/I нет.
- Знак ₽ лежит в подмножестве `latin-ext`, его нельзя отключать при оптимизации импорта. Знаки `$`, `€` и `¥` (в «CN¥») — в подмножестве `latin`.
- Стрелки → и знака ≠ нет в подмножествах `latin`, `latin-ext`, `cyrillic`. В интерфейсе вместо них иконки Lucide `arrow-right` и `equal-not`; в тексте допустим системный фолбэк.

**Моноширинный — JetBrains Mono** (SIL OFL 1.1), `@fontsource/jetbrains-mono` 5.3.0, кириллица есть. Для хэшей, идентификаторов выпусков, путей, JSON. В этой сборке 0 с точкой внутри (у глифа три контура, у O — два), а 1, l и I различаются по форме. Проверено fontTools и рендером; на 12 px точка в нуле едва заметна. В Inter 0 и O различаются только шириной, а l и I одинаковы, поэтому идентификаторы Inter не набираются.

```css
:root {
  --font-sans: 'Inter Variable', system-ui, -apple-system, 'Segoe UI', Roboto, Arial, sans-serif;
  --font-mono: 'JetBrains Mono', ui-monospace, 'Cascadia Mono', Consolas, monospace;
}
```

Имена семейств `'Inter Variable'` и `'JetBrains Mono'` взяты из CSS пакетов Fontsource. OFL требует, чтобы вместе со шрифтом распространялась лицензия: на этапе 02 нужно добавить уведомления о сторонних лицензиях в сборку.

### 5.2. Шкала

| Токены | Размер / интерлиньяж | Вес | Применение |
|---|---|---|---|
| `--text-xs` / `--leading-xs` | 12 / 16 px | 400–500 | Метаданные цитат, счётчики, надстрочные подписи |
| `--text-sm` / `--leading-sm` | 13 / 18 px | 400–500 | Плотные таблицы (≥1280), бейджи |
| `--text-md` / `--leading-md` | 14 / 20 px | 400 | Основной текст на планшете и десктопе, кнопки |
| `--text-base` / `--leading-base` | 16 / 24 px | 400 | Основной текст на смартфоне; **все поля ввода на всех ширинах** |
| `--text-lg` / `--leading-lg` | 18 / 26 px | 600 | Заголовки секций и карточек |
| `--text-xl` / `--leading-xl` | 20 / 28 px | 600 | Заголовок страницы на смартфоне |
| `--text-2xl` / `--leading-2xl` | 24 / 32 px | 600 | Заголовок страницы на планшете и десктопе |

Веса: 400 — текст, 500 — подписи и бейджи, 600 — заголовки, кнопки и ключевые суммы. 700 и выше в интерфейсе не используются.

### 5.3. Правила

- `input`, `select`, `textarea`: `font-size: 16px` на любой ширине, иначе iOS и Android масштабируют форму.
- `font-variant-numeric: tabular-nums` у всех чисел в таблицах, у денег, количеств, дат, счётчиков «N из M», номеров ревизий.
- Заглавные буквы — только в надстрочных подписях 12 px с `letter-spacing: 0.04em`. Заголовки капсом не набираются.
- Длина строки прозы (описания, цитаты) не больше 72ch.
- `<html lang="ru">`. В узких карточках `hyphens: auto`, для длинных идентификаторов и путей `overflow-wrap: anywhere`. `word-break` не используем.

---

## 6. Числа и деньги

Деньги считает сервер с decimal по семантике TenderHub (I10). Интерфейс только форматирует и никогда не пересчитывает суммы из отображаемых строк.

### 6.1. Форматирование

Только через `Intl` с локалью `ru-RU`, самописного форматирования нет. Фактический вывод проверен в Node 24.14.1 (ICU 78.2):

| Вызов | Результат | Примечание |
|---|---|---|
| `new Intl.NumberFormat('ru-RU', { style: 'currency', currency: 'RUB' })` | `1 234 567,89 ₽` | Разделитель групп U+00A0, запятая, U+00A0 перед ₽ |
| то же с `currencyDisplay: 'code'` | `-1 234 567,80 RUB` | При нескольких валютах на экране; минус — U+002D |
| `currency: 'USD'` / `'EUR'` / `'CNY'` | `-1 234 567,80 $` / `-1 234 567,80 €` / `-1 234 567,80 CN¥` | Символьный вид; в интерфейсе для иностранных валют не используется (§6.2) |
| то же с `currencyDisplay: 'code'` | `-1 234 567,80 USD` / `-1 234 567,80 EUR` / `-1 234 567,80 CNY` | Вид для иностранных валют |
| `{ minimumFractionDigits: 2, signDisplay: 'exceptZero' }` | `+1 250 000,00` / `-1 250 000,00` | Знак у разниц; минус — U+002D |
| `{ style: 'percent', maximumFractionDigits: 1 }` | `12,5 %` | U+00A0 перед % |
| `new Intl.DateTimeFormat('ru-RU', { timeZone: 'Europe/Moscow', dateStyle: 'short', timeStyle: 'short' })` | `17.09.2026, 15:05` | Время в Europe/Moscow (спецификация, §12); «МСК», где важно |

### 6.2. Правила показа

- **Точность.** Суммы всегда с двумя знаками. Сокращения «тыс.» и «млн» допустимы только в сводных плитках, и точное значение стоит рядом. В таблицах сокращений нет.
- **Валюта** видна всегда и берётся из данных. Валюты портала — рубли и USD, EUR, CNY (ответ владельца). Рубли показываются знаком ₽, иностранные валюты — кодом ISO (`USD`, `EUR`, `CNY`): «$» и «CN¥» читаются неоднозначно. Если валюты в таблице смешаны, код стоит у каждой ячейки, а итог по разным валютам не складывается.
- **Курс.** У суммы, пересчитанной в рубли, виден курс и его источник: «по курсу тендера: 1 USD = 92,50 ₽». Курс берётся из данных расчёта, портал его не подставляет.
- **НДС** показан явно: «с НДС», «без НДС», «НДС не облагается». Ставка берётся из данных, а не из кода. Метка стоит у значения или в заголовке однородной колонки. Если признак неизвестен, это состояние «Нет данных» из §4.6: подпись «НДС не указан» и `file-question-mark`. Пустое место не оставляем.
- **Вид цены.** «Себестоимость» (с бейджем «Внутреннее», `eye-off`) и «Коммерческая цена» никогда не стоят в одной колонке. Вид указан в заголовке, сравниваются только цены одного вида (I10).
- **Единицы.** Отдельная колонка или суффикс через U+00A0: `м³`, `м²`, `т`, `шт.`, `компл.`. Цена за единицу: `7 980,00 ₽ за м³ без НДС`.
- **Разницы денег** нейтральны: цвет `--text`, знак обязателен. Красный и зелёный не используются, потому что рост цены не всегда плох. Цвет есть только у строк сравнения «добавлено/удалено/изменено» (§9.6).
- **Источник значения.** У числа, пришедшего из системы или документа, есть кнопка `link-2` (16 px, на сенсорных экранах зона нажатия 44 px). Всплывающее окно показывает систему, ревизию, строку или страницу, время и автора ручного изменения. Ручная правка помечена `pencil`. Число, извлечённое моделью из документа, оформлено как производное (§4.5), пока его не подтвердит человек.
- **Разложение изменения цены** (спецификация, §10). Строки сходятся с итогом. Строка «Необъяснённый остаток» (`circle-question-mark`) видна всегда, даже при 0,00 ₽.
- **Округление** только при показе. Если показанные части не сходятся с итогом из-за округления, выводим строку «Разница округления 0,01 ₽», а не подгоняем числа.
- **Копирование.** Кнопка копирует значение без разделителей групп, с десятичной запятой; валюта и НДС копируются отдельно. Совместимость с Excel у владельца проверить на этапе реализации.
- **Даты.** Абсолютные: `17.09.2026 15:05`. Относительное «5 мин назад» — только вторично, с абсолютной датой во всплывающей подсказке.

---

## 7. Сетка, отступы, радиусы, тени, брейкпоинты

### 7.1. Токены

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;

  --radius-sm: 4px;   /* чекбокс, фокус ячейки */
  --radius-md: 6px;   /* поля ввода, бейджи */
  --radius-lg: 8px;   /* кнопки, карточки, баннеры */
  --radius-xl: 12px;  /* диалоги, нижний лист */
  --radius-full: 999px; /* чипы типов данных */

  --control-height: 36px;       /* кнопки и поля при точном указателе */
  --control-height-touch: 44px; /* минимум при pointer: coarse */
}
```

Тени `--shadow-1` и `--shadow-2` зависят от темы (§3.2). Карточки лежат на `--bg` с `--shadow-1`, всплывающие элементы — с `--shadow-2`. Других теней нет.

### 7.2. Брейкпоинты (mobile-first)

База — 360 px, дальше только `min-width`. CSS-переменные в `@media` не работают, поэтому значения пишутся числами.

| Порог | Устройства | Раскладка |
|---|---|---|
| База (от 360 px) | Android 360–412 (Galaxy A, Pixel) | Одна колонка. Строки таблиц превращаются в карточки. Нижняя панель действий. Поля по краям 16 px |
| `@media (min-width: 390px)` | iPhone 12–15 | То же; две ключевые метрики в строку |
| `@media (min-width: 430px)` | iPhone 15 Pro Max | То же; до трёх метрик в строку |
| `@media (min-width: 768px)` | iPad | Две панели (список и детали). Таблицы с горизонтальной прокруткой внутри контейнера. Поля 24 px |
| `@media (min-width: 1280px)` | Десктоп | Боковая навигация 240 px (сворачивается до 64). Плотные таблицы. Сравнение бок о бок. Поля 32 px |

### 7.3. Safe area, высоты, тап-цели

```css
.appBar {
  position: sticky;
  top: 0;
  padding-top: env(safe-area-inset-top);
  padding-inline: max(var(--space-4), env(safe-area-inset-left)) max(var(--space-4), env(safe-area-inset-right));
}

.bottomActions {
  position: fixed;
  inset-inline: 0;
  bottom: 0;
  padding: var(--space-3) var(--space-4) max(var(--space-3), env(safe-area-inset-bottom));
}

.page {
  min-height: 100dvh;
}

.scrollArea {
  overflow: auto;
  overscroll-behavior: contain;
}

@media (pointer: coarse) {
  .button,
  .iconButton {
    min-width: var(--control-height-touch);
    min-height: var(--control-height-touch);
  }
}
```

- `viewport-fit=cover`; высоты только `dvh` или `svh`, `100vh` не используем.
- Тап-цели ≥44×44 px (лучше 48) при `pointer: coarse`. При мыши элемент может быть 32–36 px, но зона нажатия не меньше 24×24.
- Горизонтальной прокрутки страницы на 360 px нет. Прокручиваются только таблицы внутри своих контейнеров.

### 7.4. Таблицы и карточки

- **≥1280 px — плотная таблица.** Строка 32 px (компактно) или 40 px (переключатель плотности). Шапка 32 px, липкая, на `--surface-2`. Первая колонка липкая. Только горизонтальные линии `--border-subtle`, зебры нет. Наведение — `--surface-2`. Выбранная строка — `--accent-soft` плюс отмеченный чекбокс, не только цвет. Числа выровнены вправо, `tabular-nums`, единицы в заголовке однородной колонки.
- **768–1279 px** — таблица с прокруткой внутри контейнера и тенью у края прокрутки. Вторичные колонки уходят в раскрываемую строку.
- **<768 px — карточка вместо строки.** Заголовок (позиция), сетка «термин — значение» в 2 колонки (число, единица, НДС), бейджи статусов, ссылки на источник. Действия — в нижнем листе через `ellipsis-vertical`. Сравнение: «было» над «стало», с маркерами.

---

## 8. Иконки

**Библиотека — Lucide** (ISC). Проверены пакеты `lucide-static` 1.47.0 и `lucide-react` 1.47.0 в npm. Иконки импортируются поимённо (`import { Stamp } from 'lucide-react'`) и попадают в сборку, спрайтов и CDN нет.

- Сетка 24×24, штрих 2, скругления по умолчанию Lucide, цвет `currentColor` из токена.
- Размеры: 16 — таблицы, бейджи, метаданные; 20 — кнопки, меню, баннеры; 24 — заголовки страниц; 32 — пустые состояния.
- Штрих 2 на всех размерах: на 16 px это ≈1,33 px, читаемость проверена рендером. `absoluteStrokeWidth` не смешиваем.
- Иконка статуса всегда стоит с подписью. У кнопок-иконок есть `aria-label`, всплывающая подсказка и зона 44 px на сенсорных экранах.
- Собственная иконка — только если в Lucide нет нужной. Рисуем по тем же правилам: сетка 24, штрих 2, скруглённые концы.
- Все имена ниже проверены как канонические в `icon-nodes.json` lucide-static 1.47.0. Часть старых имён там — псевдонимы, их не используем: `history` → `rotate-ccw-clock`, `file-question` → `file-question-mark`, `file-warning` → `file-exclamation-point`.

**Закреплённые значения.** Каждая иконка из таблицы закреплена за своим значением и в другом смысле не используется.

| Иконка | Значение |
|---|---|
| `lock` | Расчёт закрыт |
| `stamp` | Согласовано / «Согласовать…» |
| `package-check` | «Выпустить…» |
| `hard-drive-upload` | Размещено |
| `send` | Отправлено заказчику |
| `octagon-pause` | Отправка удержана |
| `flask-conical` | Проверено на фикстурах |
| `plug-zap` | Проверено вживую |
| `unplug` | Блокировка внешней системы |
| `sparkles` | Производное от модели |
| `lightbulb` | Подсказка переговоров |
| `user-check` | Решение или подтверждение человека |
| `file-text` · `scan-text` · `audio-lines` | Текст документа · OCR · транскрипция |
| `search-x` · `circle-slash` · `link-2-off` · `file-question-mark` · `file-exclamation-point` · `equal-not` | Не найдено · не предусмотрено · нет покрытия · нет данных (в том числе «НДС не указан») · неполная обработка · расхождение подтверждено (§4.6) |
| `clock-alert` | Неактуальность: подтверждение устарело, данные на момент времени, не последняя редакция |
| `octagon-alert` · `triangle-alert` · `circle-alert` | Критичность §4.4 |
| `circle-dashed` · `circle` · `contrast` · `eye` · `undo-2` · `ban` · `circle-check-big` | Статусы §4.1–4.2 |
| `server-off` | Интеграция недоступна |
| `refresh-cw` | Доступна новая версия |
| `eye-off` | Внутреннее, не для заказчика |
| `rotate-ccw-clock` | Исторический режим |
| `file-pen-line` | Документ переименован (сравнение) |
| `link-2` · `pencil` · `circle-question-mark` | Источник значения · ручная правка (сохранённая — с автором, несохранённая — с подписью «не сохранено») · необъяснённый остаток |
| `loader-circle` | Операция выполняется (кнопка ждёт ответа сервера) |
| `monitor` · `sun` · `moon` | Тема: системная · светлая · тёмная |

---

## 9. Компоненты

### 9.1. Кнопки

| Вариант | Вид | Когда |
|---|---|---|
| Основная | Заливка `--accent`, текст `--accent-contrast` | Одно главное действие в области |
| Вторичная | `--surface`, рамка `--border`, текст `--text` | Остальные действия, в том числе «Вернуть на доработку…» (`undo-2`). Возврат — штатное решение руководителя, а не опасность, поэтому красным его не красим, чтобы не подталкивать к согласованию |
| Призрачная | Без рамки, текст `--text` | Панели инструментов, «Позже» |
| Опасная | Рамка и текст `--danger` на `--surface`. Заливка `--danger` только в диалоге подтверждения | Разрушительные действия: удалить черновик, отменить незавершённое размещение |

- Высота `--control-height` (36 px), при `pointer: coarse` — 44 px. Поля по бокам 14 px, радиус 8, иконка 20, зазор 8, текст 14/600.
- Состояния. Наведение: `--accent-hover`. Фокус: кольцо `--focus`. Загрузка: `loader-circle`, кнопка заблокирована до ответа сервера (без двойной отправки, I14). Недоступность: видимая причина рядом, а не только в подсказке.

**«Согласовать…» и «Выпустить…» — защищённые действия:**

1. **Отличимы.** Основной вариант с закреплёнными иконками (`stamp` и `package-check`). Многоточие в подписи обещает диалог. В одной области они никогда не соседствуют: согласование — у руководителя на экране кандидата, выпуск — у инженера после согласования.
2. **Размещение.** Только в зоне действий в конце экрана кандидата; на смартфоне — в липкой нижней панели. От «Вернуть на доработку…» отделены промежутком не меньше 24 px. Их нет в строках таблиц, контекстных меню, массовых действиях и горячих клавишах. Они не получают автофокус, Enter в полях их не нажимает.
3. **Права.** Кнопку видит только тот, у кого есть полномочия. Остальные видят текст «Согласует руководитель». Проверяет всё равно сервер (I12, I13).
4. **Блокеры.** Если матрица выпуска запрещает действие, кнопка недоступна и рядом виден список причин со ссылками, например «2 критических замечания не закрыты».
5. **Диалог подтверждения.** Заголовок называет объект: «Согласовать кандидат КП-2026-014, ред. 3?». Внутри точный состав: ревизия расчёта, ревизия набора источников, версии приложений, число файлов и короткие хэши, открытые замечания и принятые риски. Кнопка подтверждения повторяет глагол и объект: «Согласовать ред. 3». Начальный фокус на «Отмена», Esc закрывает диалог.
6. **Результат** — постоянный статус в цепочке (§4.3). Тост вторичен.
7. **«Принять риск…»** (спецификация, §8) — отдельное вторичное действие с обязательным полем причины. Основной кнопкой не оформляется.
8. **Согласование со смартфона обязательно** (ответ владельца). Экран кандидата и диалог подтверждения проверяются на 360, 390 и 430 px. Диалог на смартфоне — полноэкранный нижний лист. Состав кандидата (ревизии, файлы, короткие хэши, открытые замечания, принятые риски) читается без горизонтальной прокрутки, кнопка подтверждения — в липкой нижней панели над safe area.

### 9.2. Таблицы

Раскладка и адаптация — §7.4. Дополнительно:

- Сортировка: иконка и `aria-sort`. Фильтры видны чипами над таблицей.
- Прочерк «—» — только «не применимо». Отсутствие данных — метка из §4.6.
- Редактирование в ячейке — поле 16 px. Несохранённая ячейка помечена `pencil` и подписью «не сохранено».
- Длинные списки: пагинация или виртуализация. Шапка и итоговая строка остаются видимыми.

### 9.3. Бейджи и чипы

- **Бейдж статуса.** Высота 24, поля 0 8 px, радиус 6, иконка 16, текст 13/500, зазор 6. Разновидности: мягкий (фон `*-soft`, передний цвет, при необходимости сплошная рамка), пунктирный (не реализовано), штрихованный (фикстуры). Длинный текст обрезается многоточием, полный — в `title`.
- **Чип типа данных** (§4.5): высота 24, радиус `--radius-full`, текст 12/500.

### 9.4. Карточка замечания

Сверху вниз:

1. **Заголовок.** Слева бейдж критичности, затем формулировка: что не так, одной фразой. Справа статус замечания и индикатор уверенности (§4.4).
2. **Левая полоса** 4 px цвета критичности. Тело карточки на `--surface`, цвет критичности больше нигде не повторяется.
3. **Основания с двух сторон.** «Требование» и «Расчёт или поле» — две цитаты (§9.5): рядом при ≥768 px, друг под другом на смартфоне.
4. Затронутые строки и поля — ссылками.
5. **Влияние**: сумма с расчётным основанием или «Не рассчитано — нет расчётного основания».
6. Действие, ответственный, срок.
7. История решений (свёрнута): кто, когда, что решил.

Замечание, предложенное моделью, несёт чип «Предложено моделью» в стиле производного, пока человек его не принял.

### 9.5. Цитата-доказательство

- **Строка метаданных** (`--text-xs`, `--muted`): чип типа данных · документ · «ред. 3 от 12.09.2026» · «с. 12» или «л. 3» · «блок 4.2.1» или координаты · источник распознавания (текстовый слой или OCR с качеством).
- **Текст** — точная цитата в «ёлочках», `--text`. Слева линия 3 px (`--border` у текста документа, точечная у OCR и транскрипции). Не пересказывается. Длинный текст сворачивается до 6 строк с кнопкой «Показать полностью».
- **Действия.** «Открыть в документе» открывает страницу, где фрагмент обведён тем же мотивом, что в знаке: рамка 2 px `--accent` с квадратными ручками 6 px. Также «Скопировать ссылку».
- **Предупреждения.** «Не последняя редакция» (`clock-alert`, `--warning`), «Низкое качество распознавания».
- **Исторический режим** (I05): цитаты из источников вне набора выпуска не показываются.

### 9.6. Сравнение (diff)

- Колонка маркеров: «+» добавлено, «−» удалено, «~» изменено. Для скринридера — скрытый текст «добавлено», «удалено», «изменено».
- Фон строки: `--success-soft`, `--danger-soft`, `--warning-soft`. У удалённого старое значение зачёркнуто. У изменённого — «было → стало» с иконкой `arrow-right`, «было» цвета `--muted`.
- Легенда маркеров видна над сравнением. Есть фильтр «Только изменения».
- Переименованный документ — `file-pen-line` «Переименован», а не пара «удалено/добавлено». Копии одного файла из двух хранилищ — один источник с пометкой «2 копии» (спецификация, §10).
- Числовая разница — отдельной колонкой со знаком, нейтральным цветом (§6.2).

### 9.7. Тосты

- Где: снизу справа (≥768 px); на смартфоне снизу по центру, над safe area и нижней панелью. Одновременно не больше трёх.
- Состав: иконка 20, текст, необязательное действие, кнопка закрытия. Фон `--surface`, тень `--shadow-2`, радиус 8.
- Время: успех и информация — 5 с, пауза при наведении и фокусе. Ошибки висят до закрытия.
- Роли: `role="status"` для информации, `role="alert"` для ошибок.
- Тост не единственная обратная связь для согласования, выпуска, размещения и отправки. Ошибки валидации — у поля, а не в тосте.

### 9.8. Баннер «Доступна новая версия»

- PWA с `registerType: 'prompt'`. Баннер немодальный, информационного стиля: `--info-soft`, иконка `refresh-cw`. Текст «Доступна новая версия портала», кнопки «Обновить» (основная) и «Позже» (призрачная).
- Где: под шапкой (≥768 px) или над нижней панелью (смартфон).
- Автоматической перезагрузки нет. При несохранённых изменениях текст «Сохраните изменения, затем обновите», а «Обновить» после сохранения.
- После «Позже» баннер вернётся при следующем открытии портала.

### 9.9. Пустые состояния

- Иконка 32 `--muted`, заголовок `--text-lg`, пояснение с областью и одно действие.
- Три разных случая, их нельзя путать: «Ещё не запускалось», «Ничего не найдено» (запускалось, результат пуст), «Нет доступа».
- Для результатов проверки никогда не пишем «Всё хорошо». Пишем «Замечаний не найдено в проверенном объёме» и охват: набор источников, «обработано N из M стр.». Если охват неполный, оформление как у «Неполной обработки» (§4.6, I18).

### 9.10. Деградация интеграции

- **Баннер** на затронутых экранах: `--warning-soft` с `server-off`, для `BLOCKED_EXTERNAL` — `--danger-soft` с `unplug`. В нём система, с какого времени, что затронуто, время последней успешной синхронизации и ссылка на экран «Состояние интеграций».
- **Данные.** Последние данные не прячем и не выдаём за текущие: у раздела или значения метка «Данные на 13:58» (`clock-alert`).
- **Действия**, которым нужна недоступная интеграция, заблокированы с видимой причиной.
- **Экран «Состояние интеграций»**: таблица со статусами из §4.1, датой последней живой и последней фикстурной проверки (двумя разными колонками) и источником ошибки (спецификация, §12).

### 9.11. Поля ввода

- Высота 36 px (44 при `pointer: coarse`), текст 16 px, рамка 1 px `--border`, радиус 6, фокус `--focus`.
- Ошибка — рамка `--danger`, иконка и текст под полем. Одной красной рамки мало.
- Обязательное поле подписано словом «обязательно», одной звёздочки мало.

---

## 10. Движение

```css
:root {
  --duration-fast: 150ms; /* наведение, фокус, раскрытие, всплывающие окна */
  --duration-base: 200ms; /* тосты, смена страниц */
  --duration-slow: 250ms; /* выезд нижнего листа или боковой панели */
  --ease-out: cubic-bezier(0.2, 0, 0, 1); /* появление */
  --ease-in: cubic-bezier(0.4, 0, 1, 1);  /* исчезновение */
}

@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }

  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation: none;
  }
}
```

- Длительности 150–250 мс. Появление — `--ease-out`, исчезновение — `--ease-in`. Дольше 400 мс — только осознанно и с обоснованием.
- Анимируются только `transform` и `opacity`; `width`, `height`, `top` — нет.
- **Мелочи** (наведение, фокус, раскрытие, тосты) — обычный CSS `transition`, без библиотек.
- **Смена страниц** — нативный View Transitions API (`document.startViewTransition`), кросс-фейд 200 мс, где браузер поддерживает. Поддержку в Safari проверить на этапе реализации. Фолбэк — без анимации или через библиотеку проекта.
- **Библиотека** — одна на проект, окончательно в ADR этапа 01. Кандидаты: правила `CLAUDE.md` (transition.dev или аналог), Motion (`motion` 13.4.0, MIT), `@formkit/auto-animate` (0.10.0, MIT). Критерии: без CDN, размер сборки, поддержка `prefers-reduced-motion`, не мешает табличной виртуализации.
- **Запрещено:** анимированные счётчики денег и количеств; праздничные эффекты при согласовании, выпуске и отправке; анимация «успеха» до ответа сервера; бесконечные анимации, кроме индикатора загрузки.

---

## 11. Темы

- Атрибут `data-theme="light"` или `data-theme="dark"` на `<html>`. Выбор пользователя (`light`, `dark`, `system`) хранится в `localStorage` под ключом `kontur-kp:theme`. По умолчанию — системная тема.
- Переключатель из трёх вариантов: «Системная» (`monitor`), «Светлая» (`sun`), «Тёмная» (`moon`). Двухпозиционного тумблера нет.
- `color-scheme` в токенах (§3.3) переключает родные элементы и полосы прокрутки.
- **Без мигания.** Маленький скрипт в `<head>` ставится до стилей и приложения: он выставляет `data-theme` и `theme-color` ещё до отрисовки.

```html
<meta name="theme-color" content="#FFFFFF" />
<script>
  (() => {
    const THEME_COLORS = { light: '#FFFFFF', dark: '#151C23' };
    let stored = null;
    try {
      stored = localStorage.getItem('kontur-kp:theme');
    } catch {
      stored = null;
    }
    const system = matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    const theme = stored === 'light' || stored === 'dark' ? stored : system;
    document.documentElement.dataset.theme = theme;
    document.querySelector('meta[name="theme-color"]')?.setAttribute('content', THEME_COLORS[theme]);
  })();
</script>
```

Скрипт проверен в Edge (`--dump-dom`): без сохранённого выбора при светлой системе получается `light` и `#FFFFFF`; `dark` даёт `dark` и `#151C23`; `light` — `light`; мусорное значение — системная тема; эмуляция тёмной системы — `dark` и `#151C23`.

- `theme-color` = `--surface` активной темы (цвет шапки): `#FFFFFF` или `#151C23`. Приложение (хук `useTheme`) обновляет мету при переключении и подписывается на `matchMedia('(prefers-color-scheme: dark)')`, если выбран режим `system`.
- Если введут CSP без `'unsafe-inline'`, в `script-src` нужно добавить sha256-хэш этого скрипта (вычисляется при сборке).
- Обе темы проверяются по таблице §3.4, логотипам (§2.2) и экранам со статусами (§4).

---

## 12. Иконки приложения и PWA

### 12.1. Исходник и набор

Исходник один — `public/kontur-kp-favicon.svg`. Всё остальное генерирует `scripts/generate-icons.mjs` (этап 02); PNG руками не правим.

| Файл | Размер | Назначение | Как получается |
|---|---|---|---|
| `favicon.svg` | Вектор | Вкладка браузера | Исходник с `viewBox="56 56 400 400"`: рисунок тот же, поля плитки обрезаны, на 16 px лист ≈9×13 px вместо ≈7×10 (сравнено рендером) |
| `favicon-32.png` | 32×32 | Вкладка (фолбэк) | Исходник целиком: линии на сетке 16 дают чёткий рендер |
| `apple-touch-icon.png` | 180×180 | iOS/iPadOS «На экран Домой» | Исходник |
| `apple-touch-icon-120.png`, `-152.png`, `-167.png` | 120, 152, 167 | iPhone, iPad, iPad Pro | Исходник |
| `icon-192.png` | 192×192 | Android/PWA, `purpose: any` | Исходник |
| `icon-512.png` | 512×512 | PWA, `purpose: any`, заставка | Исходник |
| `icon-512-maskable.png` | 512×512 | PWA, `purpose: maskable` | Исходник: значимая часть уже в круге 80 %, поля по краям ≥10 % |

Все PNG непрозрачны, фон `#0F5B6E` совпадает с `background_color`.

### 12.2. `public/manifest.json`

```json
{
  "name": "Контур КП",
  "short_name": "Контур КП",
  "description": "От исходных данных до согласованного предложения",
  "lang": "ru",
  "start_url": "/",
  "scope": "/",
  "display": "standalone",
  "background_color": "#0F5B6E",
  "theme_color": "#0F5B6E",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
    { "src": "/icon-512-maskable.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" },
    { "src": "/favicon.svg", "sizes": "any", "type": "image/svg+xml", "purpose": "any" }
  ]
}
```

`theme_color` манифеста — фирменный, он виден на заставке до загрузки страницы. После загрузки цвет задаёт `<meta name="theme-color">` по активной теме (§11).

### 12.3. `index.html` (фрагмент `<head>`)

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
<link rel="icon" href="/favicon.svg" type="image/svg+xml" />
<link rel="icon" href="/favicon-32.png" sizes="32x32" type="image/png" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
<link rel="manifest" href="/manifest.json" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="default" />
<meta name="apple-mobile-web-app-title" content="Контур КП" />
<!-- затем theme-color и скрипт темы из §11 -->
```

Поведение `apple-mobile-web-app-status-bar-style="default"` в тёмной теме iOS нужно проверить на реальном iPhone.

### 12.4. Генератор `scripts/generate-icons.mjs` (этап 02)

Зависимость — `sharp` (devDependency), команда — `npm run icons:generate`. Эскиз ниже уже запускался во временной папке (sharp 0.35.4, libvips 8.18.6, librsvg 2.62.91) на копии исходника: все 9 файлов созданы, размеры совпали, альфа-канала нет. В репозиторий он попадёт на этапе 02.

```js
// Генерирует иконки приложения из public/kontur-kp-favicon.svg.
// Запуск: npm run icons:generate
import sharp from 'sharp';
import { readFile, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const publicDir = join(dirname(fileURLToPath(import.meta.url)), '..', 'public');
const SOURCE_FILE = 'kontur-kp-favicon.svg';
const SOURCE_VIEWBOX = 'viewBox="0 0 512 512"';
const TAB_VIEWBOX = 'viewBox="56 56 400 400"';
const SOURCE_SIZE = 512;
const BACKGROUND = '#0F5B6E'; // = background_color в manifest.json

const PNG_TARGETS = [
  { file: 'favicon-32.png', size: 32 },
  { file: 'apple-touch-icon-120.png', size: 120 },
  { file: 'apple-touch-icon-152.png', size: 152 },
  { file: 'apple-touch-icon-167.png', size: 167 },
  { file: 'apple-touch-icon.png', size: 180 },
  { file: 'icon-192.png', size: 192 },
  { file: 'icon-512.png', size: 512 },
  { file: 'icon-512-maskable.png', size: 512 },
];

const svg = await readFile(join(publicDir, SOURCE_FILE), 'utf8');
if (!svg.includes(SOURCE_VIEWBOX)) {
  throw new Error(`${SOURCE_FILE}: ожидался ${SOURCE_VIEWBOX}`);
}

// Вкладка браузера: тот же рисунок, поля плитки обрезаны — на размере 16 px силуэт листа крупнее.
await writeFile(join(publicDir, 'favicon.svg'), svg.replace(SOURCE_VIEWBOX, TAB_VIEWBOX));
console.log('✓ favicon.svg');

for (const { file, size } of PNG_TARGETS) {
  const density = Math.ceil((size * 72) / SOURCE_SIZE) * 4; // растеризация с запасом ×4
  const output = join(publicDir, file);
  await sharp(Buffer.from(svg), { density })
    .resize(size, size)
    .flatten({ background: BACKGROUND })
    .png({ compressionLevel: 9 })
    .toFile(output);
  const { width, height, hasAlpha } = await sharp(output).metadata();
  if (width !== size || height !== size || hasAlpha) {
    throw new Error(`${file}: получено ${width}×${height}, alpha=${hasAlpha}`);
  }
  console.log(`✓ ${file} ${size}×${size}`);
}
```

### 12.5. Где лежат исходники

Сейчас три SVG лежат в `public/` в корне репозитория. Это временно: кода нет. Если этап 01 выберет другую структуру (например, `apps/web/public/` или `frontend/public/`), переносим все три файла в `public/` фронтенда, генератор — в его `scripts/`, и обновляем пути в этом документе. Копий в двух местах не держим.

### 12.6. Проверка на этапе 02

Lighthouse (Installable). Реальная установка на iPhone («Поделиться → На экран Домой») и на Android («Установить приложение»). Десктопная установка в Chrome и Edge. Вкладки при светлой и тёмной теме браузера.

---

## 13. Чек-лист и открытые вопросы

### 13.1. Чек-лист при реализации и ревью интерфейса

- [ ] Hex только в `src/index.css`, `manifest.json`, `index.html` (`theme-color`) и SVG-ассетах. В компонентах только `var(--…)`.
- [ ] После изменения токенов таблица контраста §3.4 пересчитана, нарушений 0.
- [ ] У каждого статуса есть подпись и иконка из §4 и §8; цвет не единственный носитель.
- [ ] `VERIFIED_FIXTURE` не зелёный, со штриховкой и колбой; в сводках считается отдельно от `VERIFIED_LIVE`.
- [ ] Цепочка выпуска: четыре отдельных шага; «Размещено» без `send` и зелёного; частичное размещение в `--warning`; удержание видно.
- [ ] Критичность и уверенность — разные элементы; уверенность монохромна; подтверждение человека с автором и датой.
- [ ] Всё модельное помечено «производное» и пунктиром `--derived`; решение человека оформлено иначе.
- [ ] «Не найдено», «Не предусмотрено», «Нет покрытия», «Нет данных», «Неполная обработка», «Расхождение подтверждено», «Подтверждение устарело» различимы и показывают обязательные сведения.
- [ ] Деньги через `Intl` `ru-RU`, `tabular-nums`, валюта, НДС и вид цены видны; есть источник значения; «Необъяснённый остаток» виден.
- [ ] «Согласовать…» и «Выпустить…»: диалог с точным составом, фокус на «Отмена», без автофокуса и горячих клавиш, блокеры видны.
- [ ] Поля ввода 16 px; тап-цели ≥44 px при `pointer: coarse`; на 360 px нет горизонтальной прокрутки; safe area учтена; высоты в `dvh`.
- [ ] Темы: скрипт в `<head>`, нет мигания, `theme-color` синхронизирован, логотип под тему.
- [ ] Движение 150–250 мс, только `transform` и `opacity`; при `prefers-reduced-motion` анимаций нет.
- [ ] Иконки приложения сгенерированы скриптом; установка PWA проверена на iPhone, Android и десктопе.
- [ ] Согласование со смартфона проверено на 360, 390 и 430 px: состав кандидата читается, подтверждение доступно без горизонтальной прокрутки.
- [ ] Иностранные валюты показаны кодом ISO, у пересчитанных сумм виден курс и его источник.
- [ ] Бренд портала не попадает в файлы для заказчика.

### 13.2. Что проверено в версии 1.0

| Проверка | Как | Результат |
|---|---|---|
| Валидность SVG | Python 3.14 `xml.etree` на файлах в `public/` | Все три разобраны. Теги только `svg`, `title`, `rect`, `path`, `g`. Нет `<text>`, `<image>`, `<style>`, внешних ссылок |
| Отрисовка файлов | Edge headless (`--headless=new --screenshot`) | Знак: 192/64/32/16 px на `#F4F6F8`, 64/32/16 px на `#0E1318`, круглая маска 80 %. Светлый логотип: 96/32/24 px на `#FFFFFF`, 48/32 px на `#F4F6F8`. Тёмный: 96/32/24 px на `#0E1318`, 48/32 px на `#151C23`. Всё чисто и читаемо |
| Растеризация как у генератора | sharp 0.35.4 / librsvg 2.62.91, увеличенные фрагменты 16 и 32 px | 32 px: строки, контур и фрагмент чёткие, у ручек мягкие края; 16 px: силуэт листа с полосой. PNG без альфа-канала |
| Укрупнённый favicon и монохром | Временные рендеры sharp | С `viewBox="56 56 400 400"` лист на 16 px крупнее (≈9×13 px вместо ≈7×10); монохром читается на белом, тёмном и жёлтом |
| Генератор иконок | Эскиз §12.4 во временной папке | 9 файлов, размеры верны, `hasAlpha = false` |
| Контраст | Скрипт WCAG (приложение A) | 40 пар × 2 темы, нарушений 0 |
| Семантика в интерфейсе | Временная HTML-витрина по правилам §4 и §9 (токены, иконки Lucide, Inter), обе темы, Edge | Статусы §4.1–4.7, кнопки, цепочка с удержанием, карточка замечания, типы данных, сравнение, тост, баннеры, пустое состояние с неполным охватом различимы; цвета спокойные |
| Скрипт темы | Edge `--dump-dom`, 5 сценариев | Ожидаемые `data-theme` и `theme-color` (§11) |
| Форматы `Intl` | Node 24.14.1, ICU 78.2 | Строки из §6.1, включая USD, EUR, CNY в символьном и кодовом виде (версия 1.1) |
| Покрытие знаков валют | `unicode-range` в CSS `@fontsource-variable/inter` 5.3.0 | `$`, `¥`, `€` — подмножество `latin`; ₽ — `latin-ext` (версия 1.1) |
| Шрифты | fontTools 4.63 на файлах Fontsource 5.3.0, рендер в Edge | Функции, оси и покрытие глифов — как в §5.1 |
| Имена иконок | `icon-nodes.json` lucide-static 1.47.0 | Все имена из §4 и §8 канонические |

**Не проверялось:** установка PWA на реальных устройствах, Safari и iOS, Lighthouse, печать, режим высокой контрастности Windows, компоненты портала (кода ещё нет), товарный знак и уникальность названия.

### 13.3. Вопросы владельцу и ответы

Ответы владельца от 17.09.2026:

| № | Вопрос | Ответ | Что меняется |
|---|---|---|---|
| 1 | Корпоративный брендбук | Пока нет | Бренд портала самостоятельный. Когда брендбук появится, палитру и знак пересматриваем |
| 2 | Название «Контур КП» (есть линейка СКБ «Контур», в том числе «Контур.Закупки») | Оставляем на время разработки, затем изменим | Проверку товарного знака делаем для финального названия. При смене названия перерисовываются надпись логотипа, `manifest.json` и заголовки |
| 4 | Валюты | Евро, доллар, юань | Валюты портала — рубли и USD, EUR, CNY; ₸ не нужен. Правила показа — §6.2 |
| 6 | Согласование со смартфона | Будет | Мобильный сценарий «Согласовать…» обязателен (§9.1, п. 8; чек-лист §13.1) |

Открытые вопросы:

1. **Логотип компании.** Нужно ли показывать в портале название или логотип компании, например в шапке?
2. **Соседние системы.** Какие знаки и цвета у TenderHub, MailHub, LocalAI, RDWeb? Вкладки браузера и ярлыки не должны путаться. Соседние проекты не изучались по договорённости.
3. **НДС и виды цен.** Какие признаки НДС и виды цен есть в TenderHub? Это определит подписи из §6.
4. **Уровни критичности.** Подходят ли три уровня (§4.4) или в процессе уже есть своя шкала?
5. **Устройства.** Какие модели iPhone и Android у пользователей?
6. **Печать.** Нужны ли печатные формы экранов (например, список замечаний)? Если да, понадобится монохромная печатная тема.
7. **Библиотека анимаций.** Утвердить подход из §10 или сразу выбрать одну библиотеку в ADR этапа 01.

---

## Приложение A. Ядро скрипта контраста

Таблица §3.4 посчитана скриптом с этим ядром (формула WCAG 2.x, Python 3.14); полный список пар совпадает со строками таблицы. Фрагмент ниже запускался как есть: для двух пар из примера он выдаёт 15.21 и 3.41 (светлая тема), 15.67 и 3.58 (тёмная) — те же числа, что в таблице.

```python
import json

def channel(value: int) -> float:
    c = value / 255
    return c / 12.92 if c <= 0.04045 else ((c + 0.055) / 1.055) ** 2.4

def luminance(hex_color: str) -> float:
    h = hex_color.lstrip('#')
    r, g, b = (int(h[i:i + 2], 16) for i in (0, 2, 4))
    return 0.2126 * channel(r) + 0.7152 * channel(g) + 0.0722 * channel(b)

def contrast(a: str, b: str) -> float:
    hi, lo = sorted((luminance(a), luminance(b)), reverse=True)
    return (hi + 0.05) / (lo + 0.05)

tokens = json.load(open('palette.json', encoding='utf-8'))  # {"light": {...}, "dark": {...}} из §3.2
pairs = [('text', 'bg', 4.5), ('border', 'surface-2', 3.0)]  # … все 40 пар из §3.4
for theme in ('light', 'dark'):
    for fg, bg, need in pairs:
        ratio = contrast(tokens[theme][fg], tokens[theme][bg])
        print(theme, fg, bg, f'{ratio:.2f}', 'OK' if ratio >= need else 'FAIL')
```
