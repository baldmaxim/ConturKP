# Контур КП — реестр неизвестных этапа 01

Каждое неизвестное имеет владельца, влияние и способ изоляции. Колонка «Изоляция» отвечает на вопрос выхода этапа 01: требует ли ответ переделки ядра.

Значения колонки «Изоляция»: `адаптер` — меняется только код адаптера и контракт; `конфигурация` — меняется настройка или справочник; `правило` — меняется правило проверки или матрица блокеров; `интерфейс` — меняется отображение. `ядро` — потребует изменения модели данных или переходов состояний; таких быть не должно.

## 1. Вопросы владельцу

| ID | Вопрос | Владелец | Влияние | Изоляция | Нужен к этапу | До ответа |
|---|---|---|---|---|---|---|
| Q-01 | Что считается закрытым расчётом; кто может править после срока; необратимо ли согласование ФП; будет ли X-01 | владелец процесса + владелец TenderHub | без ответа production-выпуск опирается на `provisional`-снимок | конфигурация + правило (блокер `CALCULATION_PROVISIONAL`) | 06 | снимок с проверкой согласованности, статус `provisional` |
| Q-02 | Есть ли API RDWeb, другие варианты схемы экспорта, срок хранения crop, допустимость передачи документов | владелец процесса | автоматический конвейер распознавания | адаптер | 04 | импорт экспорта схемы 1 |
| Q-03 | «Этап КП» = одна версия тендера TenderHub? | владелец процесса | связь этапов и версий, сравнение этапов | конфигурация (`stage_calculation_source`) | 01–06 | поддерживаются обе схемы связи |
| Q-05 | Какая сумма — итог КП; как учитывать НДС, доставку, страхование, снижение, перераспределение | владелец процесса + владелец TenderHub | сверка итога и экономический отчёт | конфигурация (`kp_total_rule`) + правило | 06, 10 | хранятся все доступные составляющие, итог не выводится |
| Q-06 | Какой проект и API у сервиса переговоров, где аудио, какие права | владелец процесса | автоматический импорт переговоров | адаптер | 07 | импорт manifest |
| Q-07 | Какие ящики MailHub доступны порталу; кто делает X-03; допустима ли локальная копия писем | владелец процесса + владелец MailHub | объём и способ импорта писем | адаптер + конфигурация | 07 | EML на синтетике |
| Q-08 | Реальные формы приложений и форматы результата | владелец процесса | генерация приложений и приёмка этапа 12 | конфигурация (`template_revision`) | 11, 12 | демо-шаблон с блокером `TEMPLATE_DEMO` |
| Q-09 | Матрица блокеров и правила принятия риска | владелец процесса | какие блокеры снимаются риском | правило (таблица §11.1) | 13 | технические блокеры неотключаемы |
| Q-10 | Тестовые и production-корни Яндекс Диска и SMB, права, состав внутренних материалов | владелец процесса | размещение и приёмка этапа 14 | конфигурация (`delivery_destination`) | 14 | контракт адаптера и локальные тесты |
| Q-11 | Эталонный тендер, две отправки, известные ошибки, пороги качества | владелец процесса | пилот и оценка модели | конфигурация | 18 | синтетические сценарии |
| Q-12 | Установлены ли Codex и Cursor, какие версии | владелец процесса | live-gate чат-клиентов | адаптер + документация | 16 | протокол и контракт MCP |
| Q-13 | Согласен ли владелец LocalAI на X-04 (импорт фрагментов портала с единицей источника, фильтр по разрешённым единицам до top-k, сервисная учётка) | владелец LocalAI | доступность смыслового поиска | адаптер (ADR-008 §9) | 05 | точный и полнотекстовый поиск портала, смысловой помечен недоступным |

## 2. Технические и эксплуатационные неизвестные

| ID | Неизвестное | Владелец | Влияние | Изоляция | Нужен к этапу | До ответа |
|---|---|---|---|---|---|---|
| U-01 | Целевой ПК: CPU, RAM, диск, режим работы, наличие RTX 5050 | владелец процесса | SLO, размер хранилища, GPU-очередь | конфигурация | 01, 17 | разработка на машине без GPU; лимит GPU-заданий = 1 |
| U-02 | Доступ сотрудников: LAN или VPN, TLS, сетевое имя | владелец процесса + администратор | транспорт и cookie-политика | конфигурация | 02, 17 | HTTPS обязателен, для разработки `127.0.0.1` |
| U-03 | Размещение PostgreSQL: служба Windows или Docker | администратор | установка и резервное копирование | конфигурация | 01, 17 | PostgreSQL ≥ 16, нативная служба предпочтительна |
| U-04 | Ключ TenderHub `tenders:read` и разрешённый тендер для live-smoke | владелец процесса | подтверждение интеграции | конфигурация | 06 | contract-тесты на фикстурах |
| U-05 | LocalAI на целевом ПК: запуск, объём индекса, конкуренция за GPU с LM Studio и агентом MailHub | владелец процесса | производительность и планирование заданий | конфигурация | 05, 17 | класс `gpu` с лимитом 1 |
| U-06 | Объём данных тендера: позиции, файлы, гигабайты | владелец процесса | SLO, резервные копии, хранение | конфигурация | 17 | измерение на пилоте |
| U-07 | Квоты хранилищ, политика удержания, место для резервных копий | владелец процесса | резервное копирование и удаление | конфигурация | 14, 17 | ежедневная копия, ничего не удаляется |
| U-08 | Машина разработки: нет GPU, 12 ГБ свободно на диске | владелец процесса | нельзя проверить здесь GPU-очередь и большие архивы | конфигурация | 03–05, 17 | тесты на уменьшенных фикстурах; полные — на целевом ПК |

## 3. Внешние доработки

| ID | Система | Владелец | Влияние | Изоляция | Нужна к этапу |
|---|---|---|---|---|---|
| X-01 | TenderHub: неизменяемый снимок ревизии, статус закрытия, состояние расчёта и согласования в API, событие изменений | владелец TenderHub | production-gate выпуска | адаптер + правило | 06 |
| X-02 | TenderHub: сохранение lineage позиций между версиями | владелец TenderHub | точность сравнения этапов | адаптер + правило | 10, 15 |
| X-03 | MailHub: сервисное чтение, курсор изменений, Message-ID и хэши вложений, полная пагинация цепочки | владелец MailHub | автоматический импорт писем и подтверждений отправки | адаптер | 07, 15 |
| X-04 | LocalAI: импорт фрагментов портала с единицей источника (прогон распознавания, письмо, редакция транскрипции), фильтр по списку разрешённых единиц до top-k, сервисная учётка (`docs/contracts/adapters.md` §4) | владелец LocalAI | смысловой поиск | адаптер | 05 |
| X-05 | RDWeb: документированный API | владелец RDWeb | автоматическая постановка распознавания | адаптер | 04 |

## 4. Вывод для выхода этапа 01

Ни одно неизвестное не отнесено к классу `ядро`: доменная модель, переходы состояний и границы транзакций не зависят от ответов. Ответы меняют адаптеры, конфигурацию, правила проверок и отображение. Поэтому реализация этапов 02–03 может начинаться без ответов на Q-01…Q-13.

Риски, связанные с этими неизвестными, перечислены в `docs/discovery.md` §12 (R-01…R-06) и в отчёте этапа.
