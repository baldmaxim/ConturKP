# Контур КП — журнал дефектов

| ID | Этап | Приоритет | Триггер/влияние | Инвариант | Статус | Fix commit | Регрессия | Reviewer closure |
|---|---|---|---|---|---|---|---|---|
| R00-01 | 00 | P2 | `discovery.md` называл `POST /api/dify/retrieval` маршрутом без токена по одному исключению из middleware; этап 01 получил бы неверную карту авторизации LocalAI | — (точность инвентаризации) | CLOSED_BY_REVIEW | `e2c8812` | путь middleware → обработчик → `authorizeDifyAdapterRequest` сверен вручную в ревизии `c03a3c4`; существование новых ссылок проверяет `verify_discovery.py` | `docs/reviews/00-review-2.md` — CLOSED |
| R00-02 | 00 | P2 | `verify_discovery.py` давал PASS без образца RDWeb и падал с `FileNotFoundError` без архива API; пути были привязаны к машине Claude | I18 | CLOSED_BY_REVIEW | `e2c8812` | `test_verify_discovery.py`, 8 сценариев: все материалы, нет RDWeb, нет архива API, нет ревизии Git, режим `--docs-only`, изменённый архив, строка вне файла, явные пути | `docs/reviews/00-review-2.md` — CLOSED |
| R01-01 | 01 | P1 | проверка ссылалась только на набор редакций документов; выбранные прогоны распознавания, письма и транскрипции не закреплялись, поздний OCR мог подменить проверенный | I02, I05, I15, I18 | CLOSED_BY_REVIEW | `37305ae` | неизменяемый снимок `evidence_scope` с типизированными единицами; проверка, кандидат, манифест и исторический поиск используют его; бумажный сценарий и регрессия: walkthroughs §6; test-plan RT-01 | `docs/reviews/01-review-2.md` — CLOSED |
| R01-02 | 01 | P1 | контракт LocalAI фильтровал только по редакции документа: поздний прогон того же PDF и письма из закрытых ящиков не исключались до top-k | I05, I17 | CLOSED_BY_REVIEW | `37305ae` | единица источника у каждого фрагмента; поиск по `allowedSourceUnitIds` до ранжирования; проверка ответа по той же области; бумажный сценарий и регрессия: walkthroughs §11; test-plan RT-02 | `docs/reviews/01-review-2.md` — CLOSED |
| R01-03 | 01 | P1 | согласование и выпуск не были сериализованы с поступлением нового источника или решения; удержание создавалось фоновым заданием | I03, I04, I13 | CLOSED_BY_REVIEW | `faff07d` | барьер актуальности: `input_version`, `stage_input_event`, предикат под блокировкой этапа, порядок блокировок, свежесть каналов поступления, запрет отправки после «влияет»; бумажный сценарий и регрессия: walkthroughs §7; test-plan RT-03 | `docs/reviews/01-review-3.md` — CLOSED |
| R01-04 | 01 | P1 | хэш расчёта не включал итог, курсы и вид ревизии; дедупликация по нему теряла новый снимок и переход `provisional` → `verified` | I02, I03, I10 | CLOSED_BY_REVIEW | `37305ae` | разделение `calculation_content` и `calculation_revision`; события статуса у источника; бумажный сценарий и регрессия: walkthroughs §8; test-plan RT-04 | `docs/reviews/01-review-2.md` — CLOSED |
| R01-05 | 01 | P1 | боевой режим определялся назначениями доставки; тестовый выпуск мог стать отправкой; повтор доставки брал текущую изменяемую настройку назначения | I01, I02, I03, I14 | CLOSED_BY_REVIEW | `37305ae` | неизменяемый `mode` кандидата и выпуска; запрет отправки тестового выпуска; `compliance_incident`; версии назначений и неизменная среда; бумажный сценарий и регрессия: walkthroughs §9; test-plan RT-05 | `docs/reviews/01-review-2.md` — CLOSED |
| R01-06 | 01 | P2 | после перехвата задания старый обработчик мог выполнить heartbeat и записать результат | I14 | CLOSED_BY_REVIEW | `37305ae` | `lease_token` при каждом захвате; все записи обработчика и доменный результат условны по нему; протокол отмены и слот GPU с защитным интервалом; бумажный сценарий и регрессия: walkthroughs §10; test-plan RT-06 | `docs/reviews/01-review-2.md` — CLOSED |
| R01-07 | 01 | P2 | решение «не влияет» записывалось как `decision`, которое само порождало событие барьера и новое удержание: бесконечная цепочка блокировок | I04 | CLOSED_BY_REVIEW | `faff07d` | `decision_type = input_event_disposition` и прочие действия вокруг барьера событий не порождают (`state-machines.md` §1.1, §13); бумажный сценарий и регрессия: walkthroughs §2, §7; test-plan RT-07 | `docs/reviews/01-review-3.md` — CLOSED |
| R01-08 | 01 | P2 | идемпотентность `provisional`-ревизии искалась среди всех прежних ревизий: возврат A → B → A переиспользовал первую ревизию, без новой ревизии и события | I03, I04, I14 | CLOSED_BY_REVIEW | `faff07d` | повтор идемпотентен только относительно последней ревизии этапа; возврат создаёт новую ревизию со ссылкой на прежнее содержимое (`state-machines.md` §6; `data-model.md` §4.5); бумажный сценарий и регрессия: walkthroughs §8; test-plan RT-04 | `docs/reviews/01-review-3.md` — CLOSED |
| R01-09 | 01 | P1 | событие `import_accepted` считалось покрытым при `completed_with_errors`, если покрыты зарегистрированные редакции; отклонённые элементы (повреждённые, сверх лимита) в условии не участвовали, неполный комплект мог выйти | I04, I18 | CLOSED_BY_REVIEW | `f8a0f20` | обязательный исход каждого элемента партии: регистрация с покрытием, дубликат с привязкой, повторный импорт или решение руководителя; неразобранные элементы дают `completed_with_gaps` (`state-machines.md` §1.1, §3.1, §8); бумажный сценарий и регрессия: walkthroughs §12; test-plan RT-08 | `docs/reviews/01-review-4.md` — CLOSED |
| R02-01 | 02 | P1 | повтор команды с тем же `Idempotency-Key` возвращал сохранённые данные этапа после снятия назначения или роли: проверка прав была только внутри исполнения команды | I13 | | CLOSED_BY_REVIEW | `a32556d` | | у каждой команды обязательный шаг `authorize` — текущие права и область объекта проверяются в транзакции до выдачи сохранённого ответа (`apps/server/src/http/command.ts`); `tests/regressions-r02.test.ts` «R02-01»: снятие назначения, снятие глобальной роли, снятие роли admin, отказ в журнале | `docs/reviews/02-review-2.md` — CLOSED |
| R02-02 | 02 | P1 | администратор без назначения читал названия и сроки этапов через журнал тендера (`details` событий `stage.*`) | I13 | | CLOSED_BY_REVIEW | `a32556d` | | журнал тендера — по области: руководитель тендера видит всё, администратор без назначения — только административные действия `ADMIN_AUDIT_ACTIONS` (`packages/core/src/capabilities.ts`, `packages/db/src/audit.ts`); `tests/regressions-r02.test.ts` «R02-02»: создание и правка этапа с прежними значениями не видны администратору | `docs/reviews/02-review-2.md` — CLOSED |
| R02-03 | 02 | P2 | параллельное снятие роли admin у двух разных администраторов оставляло ноль активных администраторов | — | | CLOSED_BY_REVIEW | `a32556d` | | общая транзакционная блокировка `lockAdminSet` до блокировок строк пользователей в `PATCH /admin/users/{id}` и в bootstrap (`packages/db/src/users.ts`); `tests/regressions-r02.test.ts` «R02-03»: два запроса ждут блокировку и выполняются строго по очереди — 200 и 409, остаётся один администратор | `docs/reviews/02-review-2.md` — CLOSED |
| R02-04 | 02 | P2 | хэши паролей в base64url не читались пакетом `argon2`: заявленная в D-006 смена реализации ломала вход | — | | CLOSED_BY_REVIEW | `a32556d` | | соль и ключ — стандартный base64 без `=`; параметры PHC разбираются в любом порядке; прежние base64url-хэши проверяются и переписываются при входе без смены `row_version` (`packages/core/src/password.ts`, `routes/auth.ts`); `tests/regressions-r02.test.ts` «R02-04»: совместимость с `argon2@0.45.1` в обе стороны, переписывание при входе | `docs/reviews/02-review-2.md` — CLOSED |
| R03-01 | 03 | P1 | строку состава замороженной ревизии набора источников можно было перенести в черновик: guard проверял статус только NEW-ревизии, поэтому замороженный состав менялся | I03, I15 | CLOSED_BY_REVIEW | `67415952b58a5caf3c70c83c2e16f9ffa519ea52` | ссылка на ревизию неизменяема после вставки, статус проверяется и у OLD, и у NEW (`docs/migrations/0003_guards_frozen_and_input_version.sql`, `source_set_item_guard`); регрессия: `tests/regressions-r03.test.ts` «R03-01: строку замороженной ревизии нельзя перенести в черновик» | `docs/reviews/03-review-2.md` — CLOSED |
| R03-02 | 03 | P1 | после завершения партии импорта оставались изменяемыми поля доказательства и идентичности элемента (`tender_id`, `observed_name`, `reject_detail`, `size_bytes`, `created_at`, `resolved_by/at`), в том числе вместе с первым заданием исхода | I15, I19 | CLOSED_BY_REVIEW | `67415952b58a5caf3c70c83c2e16f9ffa519ea52` | строгий whitelist: поля идентичности неизменяемы всегда; после завершения партии допускается единственный переход `resolution` из `none` с обязательными автором и временем, `row_version` +1; поля доказательства неизменяемы; решение о неприменимости и связь повторного импорта проверяются на принадлежность элементу и тендеру (`0003`, `import_item_guard`); регрессия: `tests/regressions-r03.test.ts` «R03-02: поля доказательства и идентичности…», «исход задаётся один раз…» | `docs/reviews/03-review-2.md` — CLOSED |
| R03-03 | 03 | P1 | `tender_stage.input_version` можно было повысить прямым UPDATE без `stage_input_event`: немедленные триггеры проверяли только шаг +1 и номер события — барьер актуальности (R01-03) обходился; отложенная проверка перечитывала финальную версию этапа: два повышения в одной транзакции закрывались одним событием последнего номера, промежуточный номер оставался без события; этап можно было создать с ненулевой версией (ревью 03-2) | I04, I13 | CLOSED_BY_REVIEW | `7156f8571f5ac98f4a6ae54dc16bef6e79ad7abb` | отложенная проверка использует образ строки каждого обновления (`NEW.input_version`): у версии, установленной этим UPDATE, должно быть своё событие; событие не может опережать версию; новый этап создаётся только с версией 0 (`docs/migrations/0004_stage_version_event_continuity.sql`); регрессия: `tests/regressions-r03b.test.ts` «два повышения версии и одно событие финального номера — COMMIT отклоняется», «новый этап нельзя создать с ненулевой версией входов», «два штатных события в одной транзакции… без пропусков» | `docs/reviews/03-review-3.md` — CLOSED |
| R03-04 | 03 | P1 | heartbeat обновлял аренду задания и слот GPU разными операторами: обработчик мог считать аренду действующей после потери слота; `lockOwnedJob` владение слотом не проверял; heartbeat продлевал аренду задания, не проверяя, что слот GPU действительно продлён; `lockOwnedJob` читал слот снимком без блокировки — перехват мог произойти в середине доменной транзакции (ревью 03-2) | I14 | CLOSED_BY_REVIEW | `7156f8571f5ac98f4a6ae54dc16bef6e79ad7abb` | heartbeat одним оператором сначала продлевает слот (берёт блокировку строки), аренда задания продлевается только при продлённом слоте; доменная транзакция владельца блокирует строку слота, поэтому перехват ждёт её завершения (`packages/db/src/jobs.ts`); регрессия: `tests/regressions-r03b.test.ts` «перехват слота во время heartbeat…», «доменная транзакция владельца слота блокирует перехват…» | `docs/reviews/03-review-3.md` — CLOSED по исходным симптомам; новые проблемы протокола ведутся как R03-10 и R03-11 |
| R03-05 | 03 | P1 | `failBatch` не вызывался: после исчерпания попыток задание становилось `failed`, а партия импорта оставалась `running` — статус не соответствовал факту; при неудаче транзакции фиксации терминальной ошибки runtime всё равно выполнял отдельный `failJob`: возвращалось расхождение «задание failed, партия running» (ревью 03-2) | I18 | CLOSED_BY_REVIEW | `7156f8571f5ac98f4a6ae54dc16bef6e79ad7abb` | после неудачной фиксации доменного отказа задание не объявляется `failed`: оно возвращается в очередь с задержкой и кодом `terminal_fixation_failed`, доменное состояние не меняется (`apps/worker/src/runtime.ts`, `requeueJob`); регрессия: `tests/regressions-r03b.test.ts` «исключение в onTerminalFailure: задание не failed, доменного маркера нет, повтор возможен» | `docs/reviews/03-review-3.md` — CLOSED |
| R03-06 | 03 | P1 | корень хранилища сравнивался лексически (`resolve`), а корень наблюдаемой папки — по `realpath`: junction-alias хранилища внутри наблюдаемой папки проверку пересечения не проходил, и портал сканировал собственные каталоги | I15 | CLOSED_BY_REVIEW | `67415952b58a5caf3c70c83c2e16f9ffa519ea52` | `BlobStore` определяет канонический физический корень при инициализации; пересечение и исключение путей проверяются в одном пространстве физических путей (`packages/storage/src/index.ts`, `apps/worker/src/handlers/intake.ts`); регрессия: `tests/regressions-r03.test.ts` «R03-06: хранилище через junction внутри наблюдаемой папки не сканируется» | `docs/reviews/03-review-2.md` — CLOSED |
| R03-07 | 03 | P1 | между обходом каталога и чтением файл (или его каталог) можно было подменить ссылкой на объект вне разрешённого корня: чтение шло по пути, а не по проверенному объекту | I15, I17 | CLOSED_BY_REVIEW | `67415952b58a5caf3c70c83c2e16f9ffa519ea52` | чтение через `openVerifiedFile`: перед открытием путь не ссылка, после открытия проверяются вид объекта, физический путь внутри канонического корня, повторный `lstat`, идентификатор объекта и совпадение размера и времени; далее читается открытый дескриптор, после чтения состояние сверяется повторно (`apps/worker/src/handlers/safeRead.ts`); регрессия: `tests/regressions-r03.test.ts` «R03-07: замена каталога ссылкой во время скана…» | `docs/reviews/03-review-2.md` — CLOSED |
| R03-08 | 03 | P2 | при существующем объекте хранилища сверялся только размер: повреждённый файл той же длины считался дубликатом (ADR-003 требует сверки размера и хэша) | I15 | CLOSED_BY_REVIEW | `67415952b58a5caf3c70c83c2e16f9ffa519ea52` | при совпадении ключа пересчитывается SHA-256 существующего объекта; расхождение — явная `StorageCorruptionError`, объект не регистрируется как дубликат (`packages/storage/src/index.ts`); регрессия: `tests/regressions-r03.test.ts` «R03-08: повреждённый объект того же размера…» | `docs/reviews/03-review-2.md` — CLOSED |
| R03-09 | 03 | P2 | строка `blob` для файла наблюдаемой папки писалась вне аренды (`ctx.pool`), в отличие от импорта архива | I14 | CLOSED_BY_REVIEW | `67415952b58a5caf3c70c83c2e16f9ffa519ea52` | метаданные `blob` пишутся в транзакции под действующей арендой вместе с партией и элементами; копирование файла по-прежнему выполняется вне транзакции (`apps/worker/src/handlers/intake.ts`); регрессия: `tests/regressions-r03.test.ts` «R03-09: после потери аренды обработчик скана не создаёт строку blob и партию» | `docs/reviews/03-review-2.md` — CLOSED |
| R03-10 | 03 | P1 | разный порядок блокировок GPU: heartbeat брал `resource_slot` → `job`, а блокировка перед доменной записью — `job` → `resource_slot`; возможен цикл ожидания (ревью 03-3) | I14 | CLOSED_BY_REVIEW | `be741ffe756e612f208553153bb539ee7c504bc0` | единый порядок `resource_slot` → `job` во всех переходах: захват, продление, блокировка перед доменной записью, успех, отмена, ошибка, возврат в очередь; освобождение слота и перевод задания — одним оператором (`packages/db/src/jobs.ts`, ADR-004); регрессия: `tests/regressions-r03c.test.ts` «доменная транзакция берёт слот раньше строки задания», «heartbeat параллельно с доменной транзакцией…», «обратный порядок…» | `docs/reviews/03-review-4.md` — CLOSED |
| R03-11 | 03 | P1 | heartbeat продлевал `resource_slot` независимо от результата обновления задания: после recovery старый обработчик продлевал слот, а worker продолжал слать heartbeat после подтверждённой потери аренды (ревью 03-3) | I14 | CLOSED_BY_REVIEW | `be741ffe756e612f208553153bb539ee7c504bc0` | heartbeat — короткая транзакция «обе аренды или ничего»: проверка и блокировка слота, затем задания, затем общий новый срок; worker прекращает heartbeat после первой подтверждённой потери (`packages/db/src/jobs.ts`, `apps/worker/src/runtime.ts`); регрессия: `tests/regressions-r03c.test.ts` «после recovery старый heartbeat… не трогает слот», «после подтверждённой потери аренды worker не шлёт новые heartbeat» | `docs/reviews/03-review-4.md` — CLOSED |
| R03-12 | 03 | P2 | в отчёте этапа осталась ссылка на цепочку коммитов прошлого цикла передачи (ревью 03-3) | — | CLOSED_BY_REVIEW | `be741ffe756e612f208553153bb539ee7c504bc0` | заголовок отчёта приведён к фактической цепочке передач; правило «HEAD отличается от коммита исправлений только этим файлом» проверяется одной командой | `docs/reviews/03-review-4.md` — CLOSED |
| R03-13 | 03 | P1 | завершения задания (успех, отмена, ошибка с повтором, возврат в очередь) очищали `resource_slot` изменяющим CTE до подтверждения владения заданием: устаревший обработчик после recovery освобождал слот и обходил защитный интервал перехвата GPU (ревью 03-4) | I14 | CLOSED_BY_REVIEW | `6b25b68429ddc83c215bbe3a9b5f70ee82d2d197` | владение подтверждается до любой записи: короткая транзакция в порядке `resource_slot` → `job`, блокировка и проверка обеих строк, затем освобождение слота и перевод задания; при неудаче проверок не меняется ничего (`packages/db/src/jobs.ts`, `packages/db/src/pool.ts`, ADR-004); регрессия: `tests/regressions-r03d.test.ts` — устаревшие `succeedJob`, `confirmCancel`, `failJob`, `requeueJob` возвращают false и не трогают слот; защитный интервал не обойдён; обработчик после потери аренды не освобождает слот через onError | `docs/reviews/03-review-5.md` — CLOSED |
| R04-01 | 04 | P1 | импорт экспорта RDWeb использовал общий `readZip`, но не накладывал ограничения архива (`maxArchiveEntries`, `maxEntryBytes`, `maxArchiveTotalBytes`, `maxCompressionRatio`): PDF хешировался без верхней границы, metadata-кандидаты копились в памяти без суммарного предела, повтор имени молча перезаписывал кандидата — регрессия относительно этапа 03 | A38 | FIXED_AWAITING_REVIEW | 47676b8195065e5bf719efe53f854a3fca4428e0 | `readRdwebArchive` применяет тот же контур, что `imports.ts`: счётчик элементов, размер элемента, суммарный распакованный объём по фактически прочитанным байтам, коэффициент сжатия, отдельный бюджет `RECOGNITION_MAX_METADATA_TOTAL_MB`, отказ `archive_corrupt` при повторе имени (`apps/worker/src/handlers/recognition.ts`); регрессия: `tests/regressions-r04.test.ts` «R04-01» — 6 сценариев | `docs/reviews/04-review-1.md` — открыт |
| R04-02 | 04 | P1 | отмена задания меняла только строку `job`: `recognition_run` навсегда оставался `queued`/`running`, блокировал заморозку состава как `recognition_in_progress`, держал пару «редакция + архив», из-за чего повторная загрузка того же ZIP вечно возвращала `reused` без активного задания | I14, I18 | FIXED_AWAITING_REVIEW | ec6d86dbaf468760324e568b8bf66206941fb7b2, 925f0457afab453183af964c7d93a8811f00023f | терминальный статус `recognition_run.cancelled` (миграция 0006); отмена `queued` — в транзакции команды `POST /jobs/{id}/cancel` (`apps/server/src/jobs/cancelDomain.ts`), отмена `running` — в транзакции worker под действующей арендой (`IJobHandlerSpec.onCancel`); `claimJob` доменных объектов не трогает; регрессия: `tests/regressions-r04.test.ts` «R04-02» — 3 сценария, `tests/queue.test.ts` «worker видит флаг сразу после захвата» | `docs/reviews/04-review-1.md` — открыт |
| R04-03 | 04 | P1 | `pages_total` брался из `pages[]` файла `_blocks.json`, а сам PDF использовался только для SHA-256: экспорт на 3 страницы при настоящем PDF на 4 получал `complete` 3/3. Пустого заголовка `## Page N` хватало, чтобы страница считалась распознанной, а отсутствие `block.status` трактовалось как «распознано» | I18 | FIXED_AWAITING_REVIEW | 47676b8195065e5bf719efe53f854a3fca4428e0 | число страниц оригинала считает worker на `pdfjs-dist` (`apps/worker/src/pdfPages.ts`) и передаёт в адаптер параметром `expect.pdfPageCount`; страница признаётся распознанной только по фрагменту с непустым текстом либо по явному статусу блока; нечитаемый оригинал — отказ `pdf_unreadable` (D-012); регрессия: `tests/regressions-r04.test.ts` «R04-03» — 5 сценариев, `tests/adapters.test.ts` — 3 сценария | `docs/reviews/04-review-1.md` — открыт |
| R04-04 | 04 | P1 | роль `kontur_app` имела INSERT в `recognition_page` и `evidence_fragment` без проверки состояния родителя: завершённому прогону можно было дописать страницу или фрагмент. `partial`/`complete` объявлялись по счётчикам без сверки с фактическими строками страниц | I15, I18 | FIXED_AWAITING_REVIEW | ec6d86dbaf468760324e568b8bf66206941fb7b2 | охранник `BEFORE INSERT` для обеих таблиц: родительский прогон блокируется и обязан быть `running`, поэтому вставка сериализуется с финальной транзакцией worker; терминализация сверяет `pages_total`/`pages_recognized` с фактическими `recognition_page` (миграция 0006); регрессия: `tests/regressions-r04b.test.ts` «R04-04» — 5 сценариев | `docs/reviews/04-review-1.md` — открыт |
| R04-05 | 04 | P1 | `evidence_fragment.document_revision_id` имел простой FK на `document_revision (id)`: строка могла ссылаться на редакцию чужого тендера при корректных `run_id` и `tender_id`, и `GET /evidence/{id}` отдавал `documentId`/`contentUrl` чужой редакции | I15, I17 | FIXED_AWAITING_REVIEW | ec6d86dbaf468760324e568b8bf66206941fb7b2 | уникальный ключ `recognition_run (id, document_revision_id, tender_id)` и составной FK `evidence_fragment (run_id, document_revision_id, tender_id)` на него; прогонный фрагмент обязан иметь редакцию (`evidence_fragment_revision_shape`) — иначе MATCH SIMPLE не проверял бы ключ (миграция 0006); регрессия: `tests/regressions-r04b.test.ts` «R04-05» — 4 сценария | `docs/reviews/04-review-1.md` — открыт |
| R04-06 | 04 | P2 | текст длиннее `maxFragmentChars` сохранялся усечённым (`slice`) с предупреждением `text_truncated`, и `text_sha256` считался по обрезку: доказательство переставало быть полным текстом блока, а этап 05 проиндексировал бы обрезок. Ключ orphan-фрагмента содержал число уже собранных фрагментов и потому был нестабилен | I15 | FIXED_AWAITING_REVIEW | ec6d86dbaf468760324e568b8bf66206941fb7b2, 47676b8195065e5bf719efe53f854a3fca4428e0 | детерминированное разбиение по границе строки, иначе по кодовой точке (суррогатная пара не рвётся): части с ключами `<ключ>#pN`, колонки `part_index`/`part_total`, курсор и индекс чтения учитывают `part_index`; ключ orphan-фрагмента больше не зависит от числа фрагментов; регрессия: `tests/adapters.test.ts` «длинный фрагмент — разбиение без потерь» (склейка частей равна исходному тексту, ключи стабильны при повторном разборе) | `docs/reviews/04-review-1.md` — открыт |
