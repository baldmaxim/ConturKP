# Контур КП — повторное ревью этапа 01, №4

Дата: 18.09.2026. Вердикт: **PASS**.

**Этап 01 принят по коммиту `f8a0f200b75d619f81d70bdd12d9aca0c2a997b6`. Можно начинать этап 02.**

Предыдущее ревью: `docs/reviews/01-review-3.md`, коммит `faff07d`. Область: diff `faff07d..f8a0f20` и связанные правила импорта, актуальности, полноты проверок и полномочий. Проверка выполнена в отдельном detached worktree на точном HEAD; рабочее дерево чистое.

## Закрытие замечания

**R01-09 — CLOSED.**

Проверены `docs/architecture/state-machines.md` §1.1, §3.1, §8, модель `import_item`, контракт `POST /import-items/{id}/resolve`, правила полномочий ADR-006, сценарии §12 и регрессия RT-08.

Вместо проверки только зарегистрированных редакций учитывается исход каждого элемента партии. `completed_with_errors` больше не является достаточным основанием для покрытия события импорта. Отказ обработки не приравнивается к неприменимости.

| Сценарий | Вывод по правилам архитектуры |
|---|---|
| D1 зарегистрирован, D2 повреждён | D2 не имеет исхода: событие партии не покрыто, согласование/выпуск блокируются; неполнота попадает в coverage проверки |
| Ни один файл не зарегистрирован, все отклонены | Автоматического покрытия пустым набором успешных редакций нет; требуется разрешение ошибок или явное решение |
| В партии только подтверждённые дубликаты | Каждый элемент связан с существующей редакцией; при её покрытии ложной неполноты не возникает |
| Ошибка устранена повторным импортом | Старый отказ сохраняется, есть ссылка на новый элемент; покрытие зависит от включения его редакции в соответствующий снимок кандидата |
| Документ неприменим | Решение с причиной принимает руководитель через `hold.resolve`; инженер не получает право объявлять неприменимость через разрешение на повторный импорт |

Команда разрешения элемента использует блокировку этапа, If-Match и идемпотентность. Решение `import_item_disposition` не создаёт рекурсивных событий барьера. При разрешении партии целиком руководитель должен перечислить неразобранные элементы; пробелы остаются видимыми. Это явное решение, а не автоматическое признание полноты.

Завершённые проверки по-прежнему неизменяемы: устранение ошибки не должно переписывать исторический `completed_with_gaps`. Дальнейшее прохождение выпуска определяется новой проверкой либо предусмотренными правилами принятия бизнес-риска. Существующие ограничения I18 и матрица блокеров сохраняются.

## Регрессии и редакционная правка

Новых блокирующих замечаний в проверенном diff не найдено. R01-01…R01-08 остаются закрытыми на основании предыдущих ревью и проверки затронутых связей.

Манифест в таблице переходов и карте I02 теперь описан через `calculation_baseline_version` и `content_baseline_version`, согласованно с моделью. Разделение стадий расчёта, согласования, выпуска, доставки и отправки сохранено. Режим выпуска, исторические снимки, фильтр LocalAI и протокол аренды не изменены.

## Выполненные проверки

| Проверка на f8a0f20 | Результат |
|---|---|
| `python artifacts/stage-01/verify_architecture.py` | PASS: 23 результата, 27 документов; найдено 12 бумажных сценариев |
| `python artifacts/stage-01/test_verify_architecture.py -v` | 10/10 OK |
| `python artifacts/stage-00/test_verify_discovery.py -v` | 8/8 OK |
| Git HEAD/status | Точный коммит f8a0f200b75d619f81d70bdd12d9aca0c2a997b6, чистое дерево |

Полную проверку внешних материалов этапа 00 повторно не запускал: скрипт и материалы данным diff не менялись; предыдущий независимый прогон на 37305ae дал PASS. Заявленный Claude новый прогон полной проверки материалов отдельно не воспроизводился.

## Границы приёмки и следующий этап

PASS относится к архитектурному этапу 01. Это не подтверждение работающего приложения, живых интеграций, конкурентности или эксплуатационной готовности. Скрипты подтверждают структуру документов; поведение исправлений проверено разбором описанных переходов. RT-01…RT-08 и остальные приёмочные тесты должны быть реализованы и пройдены на предусмотренных этапах.

Открытые вопросы Q-01, Q-03, Q-05, Q-13 и внешние доработки сохраняются с указанными в реестре сроками; для начала каркаса и прав этапа 02 они не являются блокировкой. Ограничения provisional-расчёта, демо-шаблонов и недоступных интеграций сохраняются.

Передача Claude: сохранить этот отчёт как `docs/reviews/01-review-4.md`, отметить R01-09 как CLOSED_BY_REVIEW, этап 01 как ACCEPTED по f8a0f20 и переходить к этапу 02 по утверждённому заданию. Не объявлять архитектурные RT-тесты уже выполненными тестами реализации.

Исходники в рамках ревью не менялись. Ветки не сливались и не отправлялись; приёмка этапа не является выполненным слиянием в main.
