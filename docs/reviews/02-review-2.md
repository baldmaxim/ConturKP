# Повторное ревью этапа 02 — Контур КП

Дата: 2026-09-18. Вердикт: **PASS** в пределах этапа 02.

Предыдущее ревью: `docs/reviews/02-review-1.md`.
Было: `820fb0aafe58193466e96fc00c52576c5f3609a1`.
Проверено: `a32556d6c27f4c170571e04735725b34d045df79`, ветка `origin/stage-02`.

Ревью выполнено в отдельном detached worktree на точном коммите. Изучены полный diff исправлений, контекст обработчиков авторизации, аудита и пользователей, новые тесты, обновления ADR-006, API-контракта и отчёта этапа. Код продукта не исправлялся. Merge/push и смена статусов в репозитории не выполнялись.

## Закрытие замечаний

| ID | Статус | Основание |
| --- | --- | --- |
| R02-01 | **CLOSED** | `ICommandSpec.authorize` обязателен и вызывается до чтения сохранённого результата. Проверены все вызовы `command`: команды создания этапа, тендера и пользователя имеют проверки соответствующих прав; собственные logout/password требуют аутентифицированный контекст через общий обработчик. Регрессии подтверждают 404 после снятия назначения/роли руководителя, 403 после снятия роли admin, отсутствие сохранённых данных и аудит отказа. Повтор при сохранённых правах работает. |
| R02-02 | **CLOSED** | Область журнала вычисляется по ролям и действующему назначению. Администратору без назначения разрешён явный список административных действий. SQL применяет ограничение до сортировки/LIMIT, поэтому оно действует и при пагинации. Тесты подтверждают отсутствие названий и сроков этапов, сохранение административных событий и полный журнал для руководителя, включая назначенного администратора-руководителя. |
| R02-03 | **CLOSED** | PATCH пользователей и bootstrap используют общую транзакционную advisory-блокировку. PATCH берёт её перед строковой блокировкой и подсчётом активных администраторов; второй запрос видит результат первого. Штатный конкурентный тест прошёл. Дополнительно независимо проверены одновременное снятие обеих ролей admin и отключение обоих пользователей: в обоих случаях HTTP 200/409, остаётся один активный администратор. |
| R02-04 | **CLOSED** | Новые хэши используют стандартный base64 без padding. Проверка читает прежний алфавит и параметры в разном порядке. Регрессии подтверждают совместимость с `argon2@0.45.1` в обе стороны, отказ для неверного пароля и перепись старого хэша при успешном входе без изменения версии пользователя. Перезапись условна по прежнему хэшу, поэтому не затирает более новое значение пароля. |

Основные места исправлений: `apps/server/src/http/command.ts`, `apps/server/src/routes/{admin,auth,stages,tenders}.ts`, `apps/server/src/bootstrap.ts`, `packages/core/src/{capabilities,password}.ts`, `packages/db/src/{audit,users}.ts`.

Новых подтверждённых дефектов, препятствующих приёмке, в проверенном diff не обнаружено. Обязательные блокировки предыдущего ревью не ослаблены. Полная новая функциональность последующих этапов от этого коммита не требовалась.

## Выполненные проверки

Окружение: Windows, Node 24.13.0, PostgreSQL 16. Использован отдельный тестовый кластер `127.0.0.1:55489`, синтетические пользователи и тендеры. Production БД и реальные внешние интеграции не затрагивались.

| Проверка | Результат |
| --- | --- |
| `npm ci --ignore-scripts` | PASS |
| `npm test` | **69/69**, 6 файлов тестов; включая 10 регрессий R02 |
| `npm run typecheck` | PASS |
| `npm run build` | PASS, включая генерацию PWA |
| `npm run smoke` | **15/15** на реальных процессах server/worker и PostgreSQL |
| Независимый сценарий: два параллельных снятия admin | **200/409**, один активный администратор |
| Независимый сценарий: два параллельных отключения | **200/409**, один активный администратор |
| `git diff --check 820fb0a HEAD` | PASS |

Smoke подтвердил создание/миграцию БД, повтор миграции, защищённый bootstrap и отказ повторного запуска, демо-данные, проверку конфигурации, readiness, 401 без входа, вход, получение тендеров, раздачу интерфейса с CSP и сохранение сессии после перезапуска сервера.

## Проверка конкурентности и её пределы

Оговорка автора о тесте R02-03 корректна: новый тест проверяет очередь на общей блокировке и конечное поведение исправленного кода. Он не является детерминированным воспроизведением прежнего нарушения при удалении исправления. Прежняя гонка уже воспроизведена в ревью 02-1 посредством барьера после двух реальных чтений числа администраторов.

Для данного повторного ревью этого достаточно в сочетании с анализом порядка блокировок и состояния БД. Дополнительный независимый сценарий использовал отдельное соединение, временно удерживающее общую блокировку; после постановки двух HTTP-запросов в очередь блокировка освобождалась. Проверялось число ожидающих блокировку в конкретной тестовой БД, ответы обоих запросов и итоговое число активных администраторов. SQL-результаты не подменялись, обработчики не менялись.

Сценарий сохранён рядом как `ConturKP_stage02_review2_race.mjs`. Для воспроизведения скопировать в `runtime/review2-race.mjs` проверяемого checkout, установить зависимости, задать `KONTUR_TEST_ADMIN_URL` на отдельный тестовый кластер и выполнить `node runtime/review2-race.mjs`. Он создаёт и удаляет собственные тестовые БД.

Проверки с временным откатом исправлений, о которых сообщил автор, независимо не повторялись и не заявляются как выполненные Codex.

## Ограничения и следующий этап

- Edge UI-сценарий 12/12 независимо не повторялся. Изменение ожидания готовности страницы изучено; сборка интерфейса и серверный smoke выполнены. Это не визуальная проверка браузера.
- Установка PWA на телефоне, реальный LAN HTTPS и службы Windows на целевом ПК остаются ранее обозначенными эксплуатационными проверками. Этот PASS не закрывает их и не означает готовность к промышленному развёртыванию.
- Приёмка касается проверенного коммита этапа 02, а не будущих механизмов проверки КП, согласования и выпуска.

**Этап 02 принят; можно начинать этап 03.** R02-01…R02-04 можно перенести в CLOSED на основании этого ревью. Автоматически merge, push или обновление журнала дефектов не выполнялись. Тестовый кластер по окончании работы остановлен.
